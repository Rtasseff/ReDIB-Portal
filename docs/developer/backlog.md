# Developer Backlog

Running list of feature requests, UX polish, and known issues to address in
future batches. **This file is dynamic** — add new items as they come up,
mark items done (or remove) when they ship in a batch.

**This is the one developer doc production writes to.** A session on the
production VPS that finds a problem should add an entry here (diagnosis,
file:line, what it broke) and stop there — assigning it to a bucket, and
implementing it, happen in dev. Everything else under `docs/developer/` is
read-only context on prod; see the environment note at the top of `CLAUDE.md`.

## How to use this file

- **Add new entries** to the bottom of the relevant section. Keep each
  entry to a short title plus a one-paragraph description (what + why).
  Numbers are never reused (14, 24, 25 shipped and were removed).
- **Tag** each row with a priority (`High` / `Medium` / `Low` / `Blocked`)
  and a **Round** slot naming the **bucket** that owns it in the current
  round (`baseline`, `call-hardening`, `closeout`, `eval-reminders`,
  `release-gate`, `resolution-report`), `T3 inline` for "on `main` if a
  window opens", or `—` for deferred / not this round. Buckets are defined
  in [round-october-2026.md](round-october-2026.md). An entry raised from
  production uses `needs triage` in that column until a dev session assigns
  it a bucket — prod sets the priority, dev decides where the work lands.
  `handoff` means the handoff session does it itself on `main`: design work
  that produces a document rather than code.
- **Cross-link** to source when applicable: a test-log entry, a chat
  conversation, an issue number.
- **When picked up**: move the entry into a batch implementation plan
  (`docs/developer/batchN-implementation-plan.md`) and either delete it
  here or strike it through with a pointer to the batch. Keep the file
  short — this is a backlog, not an archive.

For comparison: `developer-notes.md` records *design decisions already
made* (with a "what to do next when ready" section). This file records
*work not yet started*.

---

## Round plan — October 2026 call (set 2026-08-18)

**The operating plan lives in [round-october-2026.md](round-october-2026.md)** — bucket
order, worktree assignments, production deadlines, settled decisions and live status.
This section is the summary; that file is what to act on.

**Goal of this round:** close out the summer call (REDIB-2601, execution
ends 2026-10-30), announce the new call in September (`announced` on
`/calls/`), open it in October, and run feasibility → evaluation →
resolution through the winter on the *existing* workflow. The portal got
good reports last call: **no structural rework, no "nice to have", unseen
edge cases stay here.** The marketing-site rebuild is parked until 2027.

Priority order = **T0** close out REDIB-2601 → **T1** bugs/hardening that
would hurt the new call → **T2** features key to the new call's success →
**T3** polish that makes the call smoother, if time remains. Within a tier,
work in the order listed; the "needed by" column is the latest sensible
landing date.

| Tier | Items (in order) | Needed by | Shape |
|---|---|---|---|
| **T0 — close out REDIB-2601** | ~~31~~ → 29 + 30 → 36 | ASAP; before 2026-10-30 | 31 **shipped** (`baseline`, merged 2026-08-18); 29+30+36 (+17, 28) in `closeout` |
| **T1 — bugs / hardening before the new call** | ~~27~~ → 28 → 17 → ~~33~~ → ~~13-min~~ → ~~9~~ | 28 and 17 with `closeout` | 27, 33, 13-min, 9 **shipped** (`call-hardening`, merged 2026-08-18); 28 and 17 remain, in `closeout` |
| **T2 — features key to the new call** | 32 → 15 → 20 | 32 before evaluator assignments (~Dec); 15 before the first evaluation completes; 20 before results are published (~Jan) | one worktree bucket each: `eval-reminders`, `release-gate`, `resolution-report` |
| **T3 — smoother call, if time** | ~~7/8~~ **shipped** (`baseline`); 35, 5, 16 are folded into buckets; 34, 26, 18, 23 stay opportunistic | 7/8 first (green baseline); rest opportunistic | folded where the files already are; 23 only if Ángel's examples arrive |
| **Deferred (—)** | 4, 12, 19, 21, 22, 6, 1, 2, 3, 10, 11 | after the winter resolution / marketing cutover | structural, nice-to-have, or blocked |

**Settled 2026-08-18** (do not reopen — rationale in
[round-october-2026.md](round-october-2026.md) § 4): reminder cadences are
60/30 for #29 and 30/30 for #30, with waitlisted *applicants* getting no
recurring reminder — only a single "not reached this call" closure email
fired by #36; #15 stays in this round on the minimal `resolutions_released`
design, with a 2026-12-05 abort date; #21 stays deferred.

**Settled 2026-08-18 (second pass, after the prod findings):** the mass
call-announcement email is **off** for this round — `CALL_ANNOUNCEMENT_EMAILS_ENABLED`
defaults `False`, announcements go out by hand with the `/calls/` link, and
re-enabling is #41, wanted before the ~2027-05 call. The call-lifecycle rework
(#40) is agreed in principle and deliberately **not** scheduled this round;
the proposal is written up in
[call-lifecycle-proposal.md](call-lifecycle-proposal.md) so it can be picked
up cold. Items #38 and #39 were folded into #41.

---

## In-flight branches (not on `main`)

Registry of active worktrees (dir, port, status): [worktrees.md](worktrees.md).

---

## Bugs & correctness

| # | Priority | Round | Item |
|---|----------|-------|------|
| 48 | High | T3 inline | **A node with no active coordinator is silently dropped from feasibility review.** `applications/views.py:~789` builds a `FeasibilityReview` per node under `if node_coordinators:` — so when a node has zero active `node_coordinator` roles, **no review row is created and nothing warns**. The application still moves to `under_feasibility_review`. Consequences: (a) that node's equipment is never assessed, and because the completion check at `:1015` counts `pending_count` over `application.feasibility_reviews.all()`, the missing row contributes nothing — the application advances to `pending_evaluation` with a silent blind spot; (b) if *no* node has a coordinator, no review exists at all, nothing ever triggers the completion check, and the application sits at `under_feasibility_review` forever with nobody notified. The public consult path already handles this correctly — `notify_node_coordinators_of_consult` returns `nodes_without_coordinator` and falls back to the ReDIB coordinator (`tests/test_public_calls.py:558`) — so the fix is to give the submit path the same fallback plus a coordinator-visible warning, not to invent a new mechanism. No test covers the zero-coordinator submit case. **Coverage verified on prod 2026-08-19 — every node has at least one active coordinator, so this is latent, not currently firing.** Kept High anyway: the failure is silent and it advances an application past a review that never happened, and the trigger is a role change — which is precisely what happens during call prep (Cristina was deactivated in `1acb6d1` three days ago). Deferred to inline rather than fixed on the spot because `closeout` is told to watch `applications/views.py`; do it once that merges. (Source: handoff session review of the 2026-08-19 prod deploy, 2026-08-19.) |
| 34 | Medium | T3 inline | **Failed emails are logged and forgotten.** `send_email_from_template` swallows SMTP errors (returns False, `EmailLog` status `failed`) with no retry and no alert. On 2026-07-10 the `acceptance_reminder` to the applicant of REDIB-2601-026 failed (connection reset) — the last reminder before that applicant's deadline — and 2601-026 is the one that expired. 369 sent / 2 failed on the call, so it is rare but the misses land on deadlines. Minimal fix: Celery `autoretry_for` on transport errors with backoff (3 tries over ~1 h); coordinator visibility can be the existing `EmailLog` admin filtered to `failed` — a dashboard panel is not needed this round. (Source: REDIB-2601 call summary, 2026-08-18.) |
| 26 | Low | T3 inline | **`ConsultRequest.emails_sent_at` means "queued", not "delivered".** `calls/views.py` stamps it whenever `.delay()` didn't raise, but `send_email_from_template` swallows missing/inactive-template and SMTP errors (returns False), so the coordinator list's "Not emailed" badge never shows for requests whose notifications actually failed. Either check the eager return value / `EmailLog` status, or rename the badge to "not queued" and add an EmailLog-backed "delivery failed" indicator. Fold into 34 if that lands. (Source: 2026-08-18 review of PR #33.) |
| 37 | Medium | T3 inline | **All three app containers race to run `migrate` on every deploy.** `docker/entrypoint.sh` is shared by `web`, `celery`, and `celery-beat` (all three `build: .`) and runs `manage.py migrate --noinput` unconditionally; all three also gate on the same `depends_on: db: condition: service_healthy`, so the health gate releases them simultaneously. On the 2026-08-18 deploy of `calls/0003` two of the three crashed with `ProgrammingError: relation "calls_consultrequest" already exists` — one container committed the `CreateModel` while the others were mid-plan. Self-heals (`restart: unless-stopped` → retry finds "No migrations to apply"), and postgres DDL is transactional so the losers roll back cleanly; schema verified consistent afterwards. Latent since `b315393` (2026-02-10) but invisible until now because almost every migration since April is a choices-only `AlterField` that `sqlmigrate` renders as literal `-- (no-op)` (communications 0004-0007, calls 0002); `calls/0003` is the first `CreateModel` since `newsletters/0001` (applied 2026-06-02, logs long rotated — may well have raced unnoticed). Why it matters now: T2 (`eval-reminders`, `release-gate`, `resolution-report`) all add models/fields, so DDL-bearing deploys are about to become routine right before the October call, and a *real* migration failure is indistinguishable in the log from this benign one. Fix: wrap the migrate step in a postgres advisory lock (`pg_advisory_lock`) so the three serialize and the losers no-op — preferred over gating migrate to `web` only, which would let celery boot against the old schema. While there, check whether `seed_email_templates` (same entrypoint, also runs 3x concurrently) is idempotent under concurrency — not verified. **Triage 2026-08-18 (handoff session):** not `call-hardening` — wrong files (`docker/entrypoint.sh`, compose) and wrong review lens; it goes inline on `main`. Deadline: in prod before the `release-gate` deploy (~2026-12), the one migration this round that lands in a live call; ideally in the pre-open batch by 2026-10-13. Note it **cannot be verified in dev** — no Docker locally — so verification is the next real deploy. (Source: 2026-08-18 deploy of PR #32/#33.) **Prod evidence 2026-08-20** (the `closeout` + `acceptance-repair` deploy): it raced again, and this time it left a permanent trace. All three containers logged `Applying applications.0014 … OK`, `communications.0008 … OK` and `communications.0009 … OK`, and `django_migrations` now holds **three rows for each of those three migrations**, stamped within 80 ms of one another (14:27:03.556 → .634). Nothing crashed — all three are choices-only `AlterField`, so the DDL really is a no-op and there was nothing for postgres to reject; each container simply INSERTed its own “applied” record. The table has **no unique constraint on `(app, name)`** — only `django_migrations_pkey` on `id` — so `MigrationRecorder.record_applied` duplicates happily. Harmless to read (`showmigrations` still prints one `[X]` per migration; Django loads the applied set as a set), but it pins down the mechanism: the guard that should have stopped this *is* the applied-list read, and all three containers read it before any of them wrote. The same fingerprint is visible in the original build — `auth.0007`–`auth.0012` each carry **two** rows from 2026-04-17 10:27:25 — so the race has been firing since day one and was only ever invisible. **This answers the open question in this entry:** `seed_email_templates` *is* safe under concurrency. `celery` won and logged `7 created, 23 updated`; `web` and `celery-beat` both logged `0 created, 30 updated`; the end state is 30 templates with no duplicates, so `update_or_create` held. It does leave a reporting trap worth writing into the deploy docs: **the “N created” line lands in whichever container wins, and that was not `web`.** § 7 of the round plan tells prod to expect “7 created”, and prod reads `web`’s log by default — where it says `0 created, 30 updated`. A verifier following the doc literally would conclude the seeding had failed. Check all three logs, or check the row count. (Source: prod deploy 2026-08-20.) **Prod 2026-08-21, the `eval-reminders` + `release-gate` deploy — this is the verification event the entry said could only happen on a real deploy, and it crashed.** First DDL-bearing migration since `calls/0003`: `calls/0004` adds four columns and runs a backfill. `celery-beat` won and applied both migrations; **`web` and `celery` both died** with `psycopg.errors.DuplicateColumn: column "resolutions_released" of relation "calls_call" already exists`, full traceback out of `migrate`, then `restart: unless-stopped` brought each back to `No migrations to apply.` Self-healed exactly as predicted, ~40 s of the deploy spent on a crash loop. Note the symptom **inverts** with the DDL: on 2026-08-20's no-op `AlterField`s all three containers succeeded and left 3 duplicate `django_migrations` rows each; here the two losers rolled back, so `calls/0004` and `communications/0010` each have exactly **one** row. Duplicate rows mean the migration was a no-op; a crash means it was real. Neither is a clean signal, which is the point. Schema verified correct on `calls_call` and `calls_historicalcall`, `migrate --check` exits 0, and the `RunPython` backfill committed once and correctly (REDIB-2601 released, REDIB-2602 gated — matching the count taken before the deploy). **The risk this entry predicted is now concrete**: a genuine migration failure and this benign one are the same traceback in the same log, and `release-gate`'s own deploy is the one the entry named as the deadline. Prod cannot distinguish them by reading the log — only by checking `migrate --check` and the schema afterwards, which is what it did here. The `seed_email_templates` winner moved too — 2026-08-20 it was `celery`, this time `celery-beat` logged the `1 created` (`draft_nudge`) while `web` and `celery` logged `0 created, 31 updated`. Confirms the reporting trap is not a fixed container: **any deploy instruction that names an expected "N created" must say to check all three logs or the row count.** (Source: prod deploy 2026-08-21.) |
| 40 | Medium | — | **Call status: one manual gate, one derived phase.** `Call.status` has two independent owners that are allowed to disagree — the dates write it (`check_call_deadlines`, plus two view-level fallbacks that run on every public page hit) and humans write it (the form dropdown, now removed by 27, and the Announce/Publish/Close actions). The public querysets filter on status *and* dates, so a call whose owners disagree falls out of the site rather than merely looking odd: `open` with a future start is in neither public list but live at its URL; `announced` with a past end needed the `2ee0c7c` guard; editing dates after announcement re-evaluates nothing (GitHub #23). Proposal agreed in principle 2026-08-18: `status` becomes coordinator-set with three values (`draft` / `active` / `resolved`) and a **derived** `phase` (`upcoming` / `open` / `closed`) computed from the dates as a property, not a stored field — which is what removes the bug class, since a stored field needs a writer and a writer can be late. Full write-up, naming changes, what disappears, cost and one recorded disagreement about the auto-open email: **[call-lifecycle-proposal.md](call-lifecycle-proposal.md)**. Not this round — it is rework with no failing user in front of it, and the October call runs fine on the current code now the dropdown is gone. Window: after this call closes (~2027-03), before the next one (~2027-05), and **before 41**, which builds on it. (Source: 2026-08-18 prod investigation.)
| 41 | Medium | — | **Re-enable call announcement emails — off since 2026-08-18, wanted before the next call (~2027-05).** `notify_call_audience` (`calls/services.py`) is gated behind `CALL_ANNOUNCEMENT_EMAILS_ENABLED`, default `False`; it logs the recipient count it skipped and sends nothing. Both call sites (Announce, `calls/views.py:471`; auto-open, `calls/services.py:97`) tell the coordinator the emails are off. **Why it was turned off** (prod investigation 2026-08-18): the audience is `User.objects.filter(receive_call_notifications=True)` — a field that defaults `True` (`core/models.py:259`) and has never been opted out of, so it is **1193 of 1193 prod accounts**, and the query never checked `is_active` (now fixed, same defect family as 19). That is a ~1200-message burst through IONOS shared SMTP at `--concurrency=2`, **twice** per call (announce, then auto-open), with no `rate_limit`, no retry (34), no bounce handling for what is certainly a large share of retired addresses, and no unsubscribe link. IONOS's bulk-mail rules are not documented anywhere in this repo and were never read; unsolicited mail at that size is what their terms expect a newsletter system with an opt-out to handle. Ryan's ruling on audience stands — **everyone who has not opted out** — but the announcement is a nice-to-have: getting the upcoming call onto `/calls/` was the part that mattered and it shipped, and in the meantime Ryan and the node coordinators mail their own contacts by hand with the `/calls/` link. **Before flipping the flag:** read IONOS's actual per-hour/per-day ceiling and bulk-mail terms; add an unsubscribe path and make `receive_call_notifications` something a user has really exercised; handle bounces / prune retired addresses; batch or rate-limit to the real ceiling (as a setting, not a constant); show the recipient count on the confirmation before the coordinator commits; and settle whether the auto-open send stays automatic at all — see the recorded disagreement in [call-lifecycle-proposal.md](call-lifecycle-proposal.md) § 5. **Build this on top of 40, not before it.** (Supersedes items 38 and 39, both raised from prod 2026-08-18 and folded in here.)
| 42 | High | T3 inline | **The 6-month publication reminder goes to applicants who are not yet allowed to report one.** `access/tasks.py send_publication_followups` emails applications with `status__in=['accepted', 'completed']`, but `PublicationForm`'s `application` queryset (`access/forms.py`) only offers `status='completed'` applications — a rule deliberately locked in by `test_form_shows_only_completed_applications`. An applicant nudged while still merely `accepted` follows the link and finds nothing to select. Either narrow the reminder to `completed`, or keep the nudge and change its wording to "once your access is complete". Pre-existing, not a regression. (Source: noticed during the `baseline` branch's work on #7, 2026-08-18.) **Raised Medium -> High and pulled inline, 2026-09-01.** The Sept-Nov beat-task audit (round plan section 4.8) found this is the **next unattended applicant email after the October window closes**: the task is not scoped to a call, it queries every application on `handoff_email_sent_at` 6 months back, so REDIB-2601's June/July handoffs start maturing in **December 2026** — while most of those grants are still `accepted` rather than `completed`, because execution only ends 2026-10-30. Each one lands the applicant on a form with nothing selectable. Wanted before December, not someday. |
| 50 | Low | — | **Two email fan-outs dedupe per (recipient, application) where they should dedupe per (recipient, node).** `send_feasibility_reminders` and `_notify_freed_capacity` (`applications/tasks.py`) loop over an application's nodes and send a node-specific email, but their `EmailLog` dedupe key omits the node — so for a coordinator holding `node_coordinator` at two of the application's nodes, the first node's send suppresses the second, permanently (the first node wins on every run). `/code-review` caught and fixed exactly this in `send_completion_reminders` during `closeout` — by grouping per recipient and listing every node in one email — but the same fix was not applied to its two siblings. **Latent today:** no user in `data/users.tsv` is node_coordinator at more than one node, so the two-node case does not currently exist. (Source: handoff-session review of PR #36, 2026-08-19.) |
| 51 | Low | — | **Raw datetimes in `context_data` outside `applications/tasks.py`.** The #28 audit run by the `closeout` session found the same pattern — a datetime object in a context dict — in two apps that bucket did not own: `evaluations/tasks.py` (`deadline` in `send_evaluation_reminders`, `notify_overdue_evaluators`, `notify_coordinator_overdue_evaluations`, `assign_evaluators_to_application`, `assign_evaluators_to_call`) and `access/tasks.py` (`deadline` in its own `process_acceptance_deadlines`, `handoff_date` in `send_publication_followups`). Harmless while every call site is synchronous, as #28 established; worth pre-formatting so a later `.delay()` conversion cannot silently change what applicants read. (Source: PR #36 body, 2026-08-19.) |
| 54 | Low | — | **An auto-closed call cannot be reopened or extended from the UI.** `check_call_deadlines` (and the view-level fallback `_auto_close_expired_calls`) moves `open -> closed` at `submission_end`, but there is no `closed -> open` path: `call_edit`'s form does not expose `status` (deliberately, #27), and pushing `submission_end` forward on an already-closed call does not reopen it. So "can we give them until Friday?" — a routine ask in this network — needs a shell. Either allow a coordinator to reopen a closed call whose `submission_end` is in the future, or make the extension edit reopen it implicitly. Related to #40's manual-gate/derived-phase split; not urgent (the October call closes ~mid-December). (Source: handoff-session audit of automatic transitions, 2026-08-20.) |
| 55 | Medium | T3 inline | **An `accepted`/`pending` application with a NULL `acceptance_deadline` is unreachable by every path.** No stalled-acceptance nag (`send_stalled_acceptance_reminders` filters `acceptance_deadline__lt=now`), no expire, no force-accept — and the applicant's accept/decline link never closes, because `acceptance_deadline_passed` returns False when the field is null. The application simply sits there forever with nobody told. **Not a regression:** the auto-expire branch `acceptance-repair` deleted filtered the same way and skipped these too. Reachable when a resolution is recorded without a `resolution_date` — `applications/services/resolution.py:274` skips the deadline assignment in that case — or via legacy rows. The refusal message now says plainly that no deadline is recorded, so a coordinator who tries to act finds out; nothing yet finds it for them. Either backfill the deadline when a resolution lands without one, or surface these on Access Tracking. (Source: `/code-review` finding 8 on PR #37, 2026-08-20.) |
| 56 | Medium | T3 inline | **The acceptance half of `applications/views.py` sends email synchronously inside `@transaction.atomic`.** `promote_waitlisted_application`, `close_out_waitlisted_application`, and the three actions added by #53 (`expire_stalled_application`, `force_accept_stalled_application`, `reinstate_expired_application`) all call `send_email_from_template(...)` directly inside an open transaction, so a rollback after the send leaves an applicant holding an email about a state change that did not happen. Deliberately left consistent rather than split three ways — convert the whole acceptance half in one pass. Natural companion to #34/#26 (failed sends are logged and forgotten), which touch the same call sites. (Source: `/code-review` finding 10 on PR #37, reported not changed.) |
| 57 | Medium | T3 inline | **Two callers stamp `handoff_email_sent_at` on a handoff email that never went out.** `send_email_from_template` catches every exception and returns `False` rather than raising, so the `try/except` around `_send_handoff_email` in `application_acceptance` (`applications/views.py:~1376`) and `promote_waitlisted_application` (`:~1580`) never fires — the timestamp is written whatever happened. Coordinators then see a handoff as sent and never retry it. #53 fixed this for `force_accept_stalled_application` (`_send_handoff_email` now returns its result and the caller checks it); the two pre-existing callers still ignore the return value and were left alone as outside that branch's ownership. Same one-line fix in both. (Source: `/code-review` finding 6 on PR #37, 2026-08-20.) |
| 58 | Low | — | **An evaluator who opts out of reminders now gets no overdue warning either.** `eval-reminders` folded the former `notify_overdue_evaluators` into `send_evaluation_reminders`'s per-evaluator digest, and the digest checks `notify_reminders` / `notify_evaluation_assigned` once before building anything. The old task sent overdue notices **unconditionally**, so an opted-out evaluator used to be warned and now is not — they can drift into the `deadline + 7` lockout in silence. Implemented as the brief specified (check preferences once, when deciding whether to build the digest at all), and flagged rather than reopened. **Latent:** nothing in the codebase auto-creates a `NotificationPreference` row — no signal, no view — so `hasattr(user, 'notification_preferences')` is False for every real account and the opt-out branch never runs today. Also mitigated: `notify_coordinator_overdue_evaluations` still tells the ReDIB coordinator about every overdue evaluation unconditionally, so nobody reaches lockout without ReDIB knowing. Fix when the preferences UI becomes real: treat an overdue notice as transactional rather than a reminder. (Source: `/code-review` finding 5 on PR #38, parked deliberately.) |
| 59 | Low | — | **`calls/services.py` duplicates the "which feasibility reviews are open" query that `applications/tasks.py send_feasibility_reminders` already owns.** #5's manual dispatch renders through the identical `feasibility_reminder` template with the same context shape, so the email is identical — only the eligibility query is a second copy, written that way because `send_feasibility_reminders` was explicitly out of scope for `eval-reminders` (owned by `acceptance-repair` at the time, and the "leave it" instruction was kept even after that branch merged mid-session). Now that both buckets are merged nothing owns that file exclusively, so the two can be collapsed into one helper the beat task and the button both call. Cosmetic until the eligibility rule changes in one place and not the other. (Source: PR #38 deviations, 2026-08-21.) |
| 60 | Low | — | **`resolutions_released_at` is stamped with the migration's own run time for backfilled calls.** `calls/migrations/0004`'s `RunPython` sets `resolutions_released_at=timezone.now()` on every call it grandfathers, so REDIB-2601 will read as "released 2026-08-2x" when its resolutions actually went out in June. The flag itself is right; only the timestamp is fiction. Leaving it `NULL` for backfilled rows would have said "released, date unknown", which is true. Cosmetic — nothing reads the field yet — but it is the kind of thing that gets quoted in a report later. Fix by nulling the backfilled timestamps, or leave it and never treat the column as evidence of when a human pressed the button. (Source: handoff-session review of PR #39, 2026-08-21.) |

## Close-out of a running call (execution phase)

| # | Priority | Round | Item |
|---|----------|-------|------|
| 44 | Low | — | **A declined instrument and an unfilled one are both stored as `hours_approved=0`, and nothing downstream can tell them apart.** Surfaced by `REDIB-2601-010` (12 h requested) and `-019` (15 h requested), which each carry a 0-hour **PET/MRI 9.4T Bruker BioSpec** line beside a trimmed but nonzero **MRI 1T Bruker ICON** line (120->32 and 40->8), both at Bio Imagen Complutense, coordinator `aac.bioimac@ucm.es`. **Confirmed 2026-08-19 (Ryan): both zeros are correct and deliberate — MRI time granted, PET/MRI declined. No backfill was run and no data change is needed; this is no longer blocked on BioImaC.** `REDIB-2601-021` did get 8/8 h on the same instrument, so it was never globally unavailable. **Neither application was ever waitlisted** — `history` shows `evaluated -> accepted` with no `pending` hop — so #31's promotion bug did not cause these. What remains is the ambiguity the episode exposed: a deliberate "not granted" and a line nobody filled in are the same value in the DB, so Access Tracking and `total_approved_hours` silently fold a declined instrument into the same shape as a data gap, and no screen marks the difference. Worth a small explicit state (or at minimum a distinct display) before the next round's resolutions. Note for whoever reuses it: the query that surfaced these was sent ad hoc in chat, **not** from `docs/handoffs/baseline.md` — that file had no query at all. It filtered on status + null/0 hours only, not on "was ever `pending`", which is why it reported these two. A corrected version scoped through `simple_history` is now recorded in `docs/handoffs/baseline.md` § Outcome on prod. (Source: prod check 2026-08-19, confirmed same day.) **Correction 2026-08-21 (cutting `resolution-report`):** this entry used to end "#20's resolution table is the natural moment to fix it" — it isn't. That table publishes application / organization / node / resolution and **no hours at all**, so there is nothing in it to disambiguate; the two items were linked by proximity, not by any shared surface. #44 needs a distinct state on `RequestedAccess` (a migration) and is explicitly out of the `resolution-report` bucket. |

## Workflow features

| # | Priority | Round | Item |
|---|----------|-------|------|
| 4 | Medium | — | **Auto-assign evaluators: confirm-and-review step before emails go out.** Today the ReDIB coordinator clicks the per-call auto-assign button and the system immediately creates `Evaluation` rows AND sends assignment emails. The assignments can be edited after, but the emails are already out — hard to walk back. Add a two-step flow: (1) auto-assign computes a *preview* (which evaluators would be assigned to which apps, with COI/area annotations); (2) coordinator reviews and tweaks; (3) coordinator clicks "Confirm and notify" which persists the assignments and triggers the emails. Affected: `evaluations/views.py:auto_assign_call`, `evaluations/tasks.py:assign_evaluators_to_call`, plus a new preview template. The confirm step should also unify the auto-assign / manual-assign / combo workflows (a bug where manual assignment didn't transition `pending_evaluation` → `under_evaluation` was fixed in `07468fc`; the confirm step should make that structurally impossible). **Report note (H):** show per-evaluator load on the preview — REDIB-2601 had 7 evaluators at 6 apps each and the other 3 at 3/2/1. **Deferred:** it reworks the assignment flow, and the one-shot flow worked last call. (Source: 2026-04-16 chat + 2026-04-17 livetest; call summary 2026-08-18.) |
| 12 | Medium | — | **Dedicated `edits_requested` application status.** When a feasibility reviewer requests edits, the application is currently shoved back into `status='draft'` and `submitted_at` is cleared (`applications/views.py:761-768`), reusing the never-submitted draft state. This caused REDIB-2601-024 to silently disappear from the coordinator status wall on `/calls/<pk>/detail/` (2026-05-19). A quick fix keeps bounced drafts visible by detecting the presence of any `FeasibilityReview` row (`calls/views.py:call_detail`); the proper fix is a distinct workflow state (e.g. `edits_requested`) in `APPLICATION_STATUSES` and updates to the bounce path, templates / status badges / role-aware filters in `applications/views.py`, `calls/views.py`, and any reports that filter on `draft`. **Report note (F):** the new state should carry its own per-application resubmission deadline — REDIB-2601-020 re-entered 20 days after `submission_end`, was evaluated at the deadline and resolved last. **Deferred:** new enum state across views/templates/reports is structural; the quick fix holds. (Source: 2026-05-19 chat; call summary 2026-08-18.) |
| 21 | Medium | — | **Public "past calls" archive with published resolution tables.** Applicants and the wider network have no way to see previous calls once they close — the `/calls/` page is oriented to what's currently open/upcoming. Add an archive of closed calls (an "Open / Past" split or a per-call public detail view on the existing calls app) where each past call shows the same details it displayed when it was posted plus the resolution table from item 20 for any call whose resolution is complete (item 36's `resolved` flag), in both English and Spanish. Design points to settle: live render vs frozen snapshot of `Call`/`Equipment` rows (a snapshot is likely more honest for a published record); public visibility and what applicant-identifying information is safe (organization-level, not person-level, by design); stable permanent URLs so the tables can be linked from redib.net and from emails. Depends on 20 and 36. **Lowered High → Medium and deferred:** publication can keep going via the manual/20 export until the marketing cutover decides where public pages live. (Source: 2026-07-23 chat.) |
| 19 | Medium | — | **Reassess `User.is_active` vs `UserRole.is_active` — one concept or two?** Every role lookup in the app filters `UserRole.is_active=True`, but only two call sites also check the account itself (`evaluations/tasks.py:418` and `evaluations/views.py:210-215`, both patched after an evaluator opted out for a session). The remaining ~20 role queries (`applications/views.py` ×11, `access/views.py` ×3, `evaluations/tasks.py:644`, `applications/services/node_resolution.py:169`, `core/views.py:54`, `core/forms.py:149`, `core/decorators.py:29`) look at the role flag only, so a deactivated **account** with a live role still receives task emails it can never act on — it just can't log in. Hit again on 2026-07-23 deactivating `bioimac@ucm.es` (BioImaC): both flags had to be set by hand, and `populate_redib_users.py:257-264` hardcodes `is_active: True` on every role it writes, so the TSV's `is_active` column can never *deactivate* a role — the row's `roles` cell had to be blanked. Decide the model and enforce it once: either (a) collapse to a single source of truth, or (b) keep both with a crisp definition and route **all** lookups through one helper (`UserRole.objects.active()` / `User.active_roles()`), plus a loader that can express role deactivation. Ban raw `.filter(role=…, is_active=True)` in app code. Related: 13. **Deferred:** cross-cutting refactor; the two-flag manual procedure is documented and works. (Source: 2026-07-23 chat + earlier evaluator opt-out.) |
| 10 | Blocked | — | **Proxy applicants: applications filled out on behalf of another PI.** Currently the application is rigidly tied to the logged-in user as the sole responsible applicant — consistent with the PDF flow and required for the legal-style checkboxes (prior contact with node, consent to share personal info, etc.). A request has come in to allow alternate names / contact / organization in the application itself. To do this cleanly we'd need a processor-vs-applicant model with separate sign-offs (analogous to legal proxy forms), which adds fields and steps not in the PDF. **Do not implement** until the network agrees on a course of action. When/if approved: design the data model so the official applicant is still a registered `User` (preserving FK chain to organization, etc.) with a distinct "submitted by" processor identity and dual sign-off fields. (Source: 2026-05-05 email to nodes.) |
| 11 | Blocked | — | **Additional application contacts (CC list) with opt-out.** Request to allow applications to carry a list of extra contact emails that get CC'ed on application correspondence. Not implemented originally because (a) we rely on email verification to confirm the recipient consents to be emailed, and (b) sending to arbitrary user-supplied addresses without an unsubscribe path edges into spam territory. If approved by the network, the feature needs an opt-out / unsubscribe mechanism for CC'd addresses and a clear scope ("contacted only during the application lifecycle"). **Do not implement** until the network agrees. Until then, nodes can continue using their existing direct-email channels to loop in collaborators. (Source: 2026-05-05 email to nodes.) |

## UX polish

| # | Priority | Round | Item |
|---|----------|-------|------|
| 23 | Low | T3 inline | **Scientific-project guidance text: worked examples per field (blocked on Ángel).** The Batch 1 requirements doc (`~/tmp/protal_issues_20260401.md`, 2026-04-01) deferred issue #25: the applicant-facing guidance/placeholder text on the Step 1/3 scientific fields (`applications/forms.py` help_texts and placeholders, `templates/applications/wizard_step1.html`, `wizard_step3.html`) was to be improved with concrete example text once Ángel supplied approved examples. Nothing has arrived and the text is unchanged; leave as-is until examples exist, then it is a copy-only change (no model/migration) — worth a nudge to Ángel before the October open. (Source: 2026-08-17 review of the April requirements doc; every other item in it shipped in `fixes-batch-1`, merged `62d9180`.) |
| 1 | Low | — | **Equipment details drill-in on calls listing.** On `/calls/`, equipment is listed by name only with no way to view details. A click or hover affordance to surface equipment specs/availability/photos would help applicants assess fit before opening a full application. (Source: localtest3 walkthrough P1.) |
| 2 | Low | — | **Role-relative status coloring.** Today the status badges on application lists use the same colors regardless of who is viewing. Red could be reserved for "this viewer needs to act on this app" rather than a globally "bad" state — so an applicant sees red on apps awaiting their response, an evaluator sees red on assignments they owe, etc. Requires per-role rendering of the shared `templates/includes/status_badge.html`. Same family as GitHub #20/#27. (Source: localtest3 walkthrough P2.) |
| 3 | Low | — | **Organization picker: search/better sort in the profile form.** The organization dropdown on `/profile/` has many options and is hard to scan. Add a typeahead/autocomplete (e.g., select2 or similar lightweight HTMX widget), or at minimum sort by frequency-of-use. (Source: 2026-04-16 chat.) |

## Tooling / data ingestion

| # | Priority | Round | Item |
|---|----------|-------|------|
| 6 | Medium | — | **xlsx → tsv conversion + QC tooling.** The reference TSVs in `data/` are produced by exporting from Microsoft Excel on Windows, which writes Windows-1252 (CP1252) text and CRLF line endings. Our loaders assume UTF-8, so non-ASCII names (e.g. `Asociación`, `Compostela`) crash the import on the first row. Today the workaround is a manual `iconv -f cp1252 -t utf-8` pass plus eyeballing column counts. Build a small `manage.py` command (or standalone script) that takes the source `.xlsx`, writes UTF-8 TSV, and runs file-specific QC. Checks observed in real data so far: encoding (force UTF-8 output); trailing-tab artifacts in headers; leading/trailing whitespace in cells; underscore-style phone-extension suffixes (e.g. `+34 91 ... _5404`) and Excel treating a leading `+` as a formula; typos in enum-valued columns (e.g. `prelcincal` for `preclinical`, `Public Research Organisation (PRO)` exact-spelling check); separator drift (`,` used where `;` is required, e.g. `users.tsv` `areas`); ambiguous booleans (blank vs explicit TRUE/FALSE); FK lookups (`organization_name` → `organizations.tsv:name`; `roles` `node_coordinator:CODE` → `nodes.tsv:code`; `equipment.tsv` `node_code` → `nodes.tsv:code`); duplicate natural keys; required columns present. Runnable per-file (`--file organizations`) and as a batch. (Source: 2026-04-16 chat / real-data prep.) |
| 22 | Low | — | **Newsletter image handling — replace the manual base64 inliner.** The `newsletters` app serves the uploaded HTML file raw in an iframe and the `Newsletter.html_file` field requires a *self-contained* file ("inline CSS, no external assets"), so the only way to include images today is to base64-embed them as data URIs inside the single HTML file. For the June 2026 issue this was done with a throwaway build script (`/home/rtasseff/tmp/redibNewsletter/build_newsletter.py`): it downscales/compresses each PNG with ImageMagick and inlines them, producing a ~1.5 MB self-contained HTML. Options: (1) a `NewsletterImage` inline model in the admin (upload images alongside the newsletter, reference by a stable `/media/...` URL — the raw iframe serves at `/newsletters/<slug>/raw/`, so images must use root-relative or absolute URLs); (2) an upload-time processing step that accepts a folder/zip and auto-inlines + optimizes (productionize the throwaway script as a `manage.py` command); (3) a richer authoring model rendered server-side. Keep the iframe `X-Frame-Options: SAMEORIGIN` serving intact and confirm images load in both dev and prod (Caddy serves `/media/`). **Lowered Medium → Low:** not call-related; the script works. (Source: 2026-06-01 chat — first newsletter, Option A chosen as the interim.) |
| 43 | Medium | — | **`data/` TSVs and the portal DB are two writers with no reconciliation.** The five `populate_redib_*` loaders treat their TSV as authoritative for every column on every run, and per the shared rule a blank cell is written as a deliberate `False`/empty. That is correct for reference data the coordination team owns outright — and wrong wherever the portal also writes. **Immediate hazard closed 2026-08-21**: `populate_redib_users` is now create-only by default (`--update-existing` opts back in), so a load can no longer deactivate a serving evaluator or clear `auto_data_consent` network-wide; see `data/README.md`. **What remains is the design question.** (a) *Users* — `phone`, `position`, `orcid`, `organization`, `auto_data_consent` are on the profile form, so the TSV is permanently stale against prod and there is no way to push a genuine correction without also reverting whatever the user typed. Create-only defers this, it does not answer it. (b) *Organizations* — an applicant can **create** an `Organization` from the profile form's "Other (create new)" path (`core/forms.py:196`, `get_or_create(name=...)`), and `populate_redib_organizations` is `update_or_create(name=..., defaults=...)`. Nothing is deleted, but the moment the coordination team adopts a user-created org into the TSV with blank cells, the VAT/address/city that applicant typed are wiped. Same class as (a), narrower trigger. (c) *Roles* — `UserRole` has no portal editor, but a superuser can change roles in Django admin; the loader only ever adds or reactivates roles, never removes, so nothing is lost — the TSV just silently stops being the record. **The real ask** (Ryan, 2026-08-21) is a reconciliation mechanism: see the differences, resolve conflicts, record when each side changed, sync regularly. Equipment/nodes/funding agencies stay TSV-authoritative and need none of this — they are also consumed outside the portal (website, reporting), which is a second reason their ownership must stay with the coordination team. Not before the October call. (Source: prod verification 2026-08-19; scoped down and partially mitigated 2026-08-21.) |
| 62 | Low | — | **Nothing on the running system says which commit the image was built from.** The prod services all `build: .` with no source bind mount, so `git log` in `/home/deploy/ReDIB-Portal/` describes the *checkout*, not the code that is serving — the two differ for the whole window between `git pull` and `up -d --build`. On 2026-08-21 that gap made a management command, re-run after a pull to verify a just-merged fix, report the pre-fix behaviour and look exactly like the fix having failed; the session only got a true answer by piping the new source into `manage.py shell` after auditing that every write in it sat behind a dry-run branch. Now documented in DEPLOYMENT.md, but documentation only helps whoever reads it first. Cheap fix: bake the SHA in at build time (`ARG GIT_SHA` → `ENV`) and surface it read-only — a `manage.py version`, or the admin footer, or both. Then "is my fix actually live?" is one command instead of a deduction. (Source: prod, 2026-08-21.) |
| 63 | Medium | — | **Effective evaluator capacity is a third smaller than the roster, and clinical is thin.** Prod counted 15 active `evaluator` UserRole rows on 2026-08-21 but only **10** on active accounts: `aarjonilla@`, `jalonsof@`, `mariaguembe@`, `mceballos@`, `morejaso@` hold live evaluator roles on deactivated users. Nothing is broken and nothing is inconsistent — it matches `data/users.tsv`, where those five have a blank or `FALSE` `is_active` cell, and the loader deliberately never revokes a role. But the number that matters for October is the intersection, and no screen shows it: effective coverage is **preclinical 10, clinical 4, radiochemistry 3**. Clinical at 4 is the one to watch, because `auto_assign_evaluators` applies a hard COI exclusion (an evaluator cannot share the applicant's organization) and a per-evaluator cap on top of that, so 4 is the ceiling before either constraint bites — and the fallback when the area-matched pool is empty is to assign a non-matched evaluator, which is a silent quality loss rather than an error. Two separate things to decide: whether the five deactivated accounts should be reactivated or their roles retired (a `UserRole` on a dead account is invisible to everything but a count), and whether the clinical pool needs recruiting before evaluator assignment in ~2026-12. Worth surfacing as a coordinator-facing capacity view rather than a shell query. (Source: prod verification 2026-08-21.) |

## Test infrastructure

| # | Priority | Round | Item |
|---|----------|-------|------|
| 47 | Low | — | **Two `EmailTemplate.TEMPLATE_TYPES` choices are never seeded and never sent.** `communications/models.py:16` defines **30** template types; `seed_email_templates` creates **28** (23 before `closeout` added five; prod confirmed 0 created / 23 updated on 2026-08-19). The two orphans are `feasibility_rejected` and `access_scheduled`, and neither is referenced anywhere in the code — feasibility rejection is actually delivered by `feasibility_complete` with `is_approved=False` (`applications/views.py:~1032`). Harmless today, and `send_email_from_template` fails loudly rather than silently if one were ever called (it writes a `failed` `EmailLog` with the missing-template name). But the mismatch reads as "two templates are missing" to anyone counting, and `closeout` (merged 2026-08-19) added five more to the same list, all of them seeded. Either delete the two dead choices (needs a migration) or seed them. (Source: handoff session review of the 2026-08-19 prod deploy report.) |
| 46 | Low | — | **`manage.py test` misses 94% of the suite.** `tests/` has no `__init__.py`, so Django's default discovery walks past it: `python manage.py test` runs **11** tests (only `reports/tests.py`, the one app-level module with real content) and exits `OK`, while the actual workflow suite needs the explicit `python manage.py test tests` — **162** tests. Anyone running the obvious command gets a green light from 6% of the suite. Likely a one-line fix (`touch tests/__init__.py`, then check nothing double-collects), but it is test infrastructure mid-round, so it is not urgent — the round plan and the handoff briefs now name both commands. (Source: handoff session, 2026-08-19, while recording the `closeout` baseline.) |

---

## Side note — GitHub issues

`gh issue list` still carries #14–#30 from the April 2026 pre-launch test
period (imported from the SharePoint list). Most predate `fixes-batch-1`
and the summer call; several duplicate rows here (#27 ≈ 16, #20 ≈ 2, #19 =
shipped waitlist flow, #23 ↔ 27). They are **not** part of this round's
plan — triage and close them in a separate pass, moving anything still
real into this file.
