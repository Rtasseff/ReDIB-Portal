# Developer Backlog

Running list of feature requests, UX polish, and known issues to address in
future batches. **This file is dynamic** — add new items as they come up,
mark items done (or remove) when they ship in a batch.

**This is the one developer doc production writes to.** A session on the
production VPS that finds a problem should add an entry here (diagnosis,
file:line, what it broke) and stop there — assigning it to a bucket, and
implementing it, happen in dev. Everything else under `docs/developer/` is
read-only context on prod; see the environment note at the top of `CLAUDE.md`.

## How to use this file

- **Add new entries** to the bottom of the relevant section. Keep each
  entry to a short title plus a one-paragraph description (what + why).
  Numbers are never reused (14, 24, 25 shipped and were removed).
- **Tag** each row with a priority (`High` / `Medium` / `Low` / `Blocked`)
  and a **Round** slot naming the **bucket** that owns it in the current
  round (`baseline`, `call-hardening`, `closeout`, `eval-reminders`,
  `release-gate`, `resolution-report`), `T3 inline` for "on `main` if a
  window opens", or `—` for deferred / not this round. Buckets are defined
  in [round-october-2026.md](round-october-2026.md). An entry raised from
  production uses `needs triage` in that column until a dev session assigns
  it a bucket — prod sets the priority, dev decides where the work lands.
- **Cross-link** to source when applicable: a test-log entry, a chat
  conversation, an issue number.
- **When picked up**: move the entry into a batch implementation plan
  (`docs/developer/batchN-implementation-plan.md`) and either delete it
  here or strike it through with a pointer to the batch. Keep the file
  short — this is a backlog, not an archive.

For comparison: `developer-notes.md` records *design decisions already
made* (with a "what to do next when ready" section). This file records
*work not yet started*.

---

## Round plan — October 2026 call (set 2026-08-18)

**The operating plan lives in [round-october-2026.md](round-october-2026.md)** — bucket
order, worktree assignments, production deadlines, settled decisions and live status.
This section is the summary; that file is what to act on.

**Goal of this round:** close out the summer call (REDIB-2601, execution
ends 2026-10-30), announce the new call in September (`announced` on
`/calls/`), open it in October, and run feasibility → evaluation →
resolution through the winter on the *existing* workflow. The portal got
good reports last call: **no structural rework, no "nice to have", unseen
edge cases stay here.** The marketing-site rebuild is parked until 2027.

Priority order = **T0** close out REDIB-2601 → **T1** bugs/hardening that
would hurt the new call → **T2** features key to the new call's success →
**T3** polish that makes the call smoother, if time remains. Within a tier,
work in the order listed; the "needed by" column is the latest sensible
landing date.

| Tier | Items (in order) | Needed by | Shape |
|---|---|---|---|
| **T0 — close out REDIB-2601** | 31 → 29 + 30 → 36 | ASAP; before 2026-10-30 | 31 in the `baseline` bucket; 29+30+36 (+17, 28) in `closeout` |
| **T1 — bugs / hardening before the new call** | 27 → 28 → 17 → 33 → 13 (minimal slice) → 9 | 27 in prod before the ~09-15 announce; 9 and 13-min in prod before the ~10-15 open | 27, 33, 13-min, 9 in `call-hardening`; 28 and 17 in `closeout` (same file it rewrites) |
| **T2 — features key to the new call** | 32 → 15 → 20 | 32 before evaluator assignments (~Dec); 15 before the first evaluation completes; 20 before results are published (~Jan) | one worktree bucket each: `eval-reminders`, `release-gate`, `resolution-report` |
| **T3 — smoother call, if time** | 7/8 and 35, 5, 16 are folded into buckets; 34, 26, 18, 23 stay opportunistic | 7/8 first (green baseline); rest opportunistic | folded where the files already are; 23 only if Ángel's examples arrive |
| **Deferred (—)** | 4, 12, 19, 21, 22, 6, 1, 2, 3, 10, 11 | after the winter resolution / marketing cutover | structural, nice-to-have, or blocked |

**Settled 2026-08-18** (do not reopen — rationale in
[round-october-2026.md](round-october-2026.md) § 4): reminder cadences are
60/30 for #29 and 30/30 for #30, with waitlisted *applicants* getting no
recurring reminder — only a single "not reached this call" closure email
fired by #36; #15 stays in this round on the minimal `resolutions_released`
design, with a 2026-12-05 abort date; #21 stays deferred.

---

## In-flight branches (not on `main`)

Registry of active worktrees (dir, port, status): [worktrees.md](worktrees.md).

---

## Bugs & correctness

| # | Priority | Round | Item |
|---|----------|-------|------|
| 31 | High | baseline | **Waitlist promotion leaves `hours_approved` = 0.** `promote_waitlisted_application` (`applications/views.py:1404`) flips status/resolution and fires the `resolution_accepted` + handoff emails, but never sets or confirms `hours_approved` on the equipment lines — it only *reads* them for the handoff context. All 7 REDIB-2601 waitlisted applications sit at 0 approved hours on every line (e.g. 2601-022: 464.5 h requested, 0 approved); promote one today and the handoff email says "approved 0 h" per instrument and the grant is recorded as zero. Fix: on promotion, require the node coordinator to confirm/enter approved hours per equipment line (default to `hours_requested`, same widget as `node_resolution/review.html`) before the emails go out; refuse to promote with all-zero hours. Blocks any promotion for the summer call and every waitlist in the new call. (Source: REDIB-2601 call summary, 2026-08-18.) |
| 27 | High | call-hardening | **`CallForm` exposes the raw `status` dropdown — coordinators can bypass the Announce/Publish/Close guards.** `calls/forms.py:18` includes `status` in `Meta.fields` and `templates/calls/call_form.html:36-37` renders it on **create** as well as edit, so the first screen a coordinator sees when making the October call can put it straight into `announced` or `open` regardless of dates or equipment. **Prod walkthrough 2026-08-18** (create a call with a future submission window) found two concrete bad states, neither of which any guard catches. (a) Picking **Announced** in the dropdown lists the call publicly and opens it to public consult requests, but sends **no `call_announced` email at all** — only the Announce action does that (`calls/views.py:471`) — and the Announce button then refuses because it requires `status='draft'` (`calls/views.py:445`), so the announcement mail for that call can never be sent without editing the status back to draft first. (b) Picking **Open** with a future `submission_start` makes the call **invisible on `/calls/`**: the open list requires `submission_start__lte=now` (`calls/views.py:80-84`) and the upcoming list only takes `status='announced'` (`calls/views.py:86-88`), so the row falls between both querysets while staying reachable at `/calls/<pk>/` and still accepting public consult requests — and it will never fire the "Now Open" email either, because `open_announced_calls` only picks up `status='announced'` (`calls/services.py:79-80`). Fix: drop `status` from the form (transitions only via the action buttons) or validate transitions in `CallForm.clean`. **Raised Medium → High (prod, 2026-08-18):** the September announce is the first time anyone creates a call on this code path, and both bad states are silent — nothing warns the coordinator. Related to GitHub #23 (what happens when call dates are edited) and subsumed by 40 if that is taken. (Source: 2026-08-18 review of PR #33; prod walkthrough 2026-08-18.) |
| 28 | Medium | closeout | **`applications/tasks.py:~338` passes datetime objects in Celery `context_data`.** The `acceptance_expired` email passes `app.acceptance_deadline` raw; Celery's JSON serializer delivers it to the worker as an ISO string, so the rendered email shows `2026-10-01T00:00:00+00:00` in prod while dev (eager) formats it. `calls/services.notify_call_audience` pre-formats dates now; do the same there and grep for other `context_data` datetimes. Tiny; applicant-facing in prod. (Source: PR #33 notes.) |
| 17 | High | closeout | **Acceptance deadline task ignores waitlisted applicants.** `process_acceptance_deadlines` (`applications/tasks.py:240`) filters `status='accepted'` only in **both** branches — the day-7 reminder (`:263`) and the day-10 auto-expire (`:315`). But `pending` (waitlist) applications carry the same `acceptance_deadline` + `accepted_by_applicant` accept/decline window and surface in the UI as "Waitlist — Awaiting Applicant" (`templates/includes/status_badge.html:27`). Result: waitlisted applicants get **no reminder email and never auto-expire** — they sit at their deadline until a coordinator acts by hand. Fix: change both filters to `status__in=['accepted', 'pending']` (and sanity-check that the reminder/expire context and any downstream waitlist-promotion logic still read correctly for `pending`). Until shipped, send waitlist reminders manually via `send_email_from_template('acceptance_reminder', …)`. **Mitigation already done (2026-06-30):** manually sent `acceptance_reminder` to the 3 waitlisted applicants in call 1 (REDIB-2601-012 / -020 / -022) — which is why all 7 responded. Raised to High: the manual workaround will not scale to the new call. (Source: 2026-06-30 chat.) |
| 33 | Medium | call-hardening | **`Call.total_approved_hours` sums `hours_requested`, not `hours_approved`.** `calls/models.py:73` and `:112`, surfaced on the coordinator call-detail page (`templates/calls/detail.html:82,139`), the admin, and `reports/utils.py:96` (which carries a comment acknowledging it). For REDIB-2601 it reports 2,176 h against a true 1,991 h; BioImaC's MRI 1T ICON shows 430 h vs 310 h actually approved — 39% over. Capacity planning off that page is wrong today. Fix the two model properties + `reports/utils.py` to sum `hours_approved` (falling back to `hours_requested` only for lines not yet resolved, if the page needs a "requested" figure keep it as a separate column). (Source: REDIB-2601 call summary, 2026-08-18.) |
| 9 | Medium | call-hardening | **Fan out feasibility-request emails to all node coordinators.** When an application is submitted, `applications/views.py:585-597` looks up node coordinators with `.first()` and assigns the `FeasibilityReview` to a single (essentially arbitrary) user, who is then the only one emailed (`feasibility_request` at line 631). If a node has multiple active `node_coordinator` roles, the others are silent. Decide the intended behavior — notify all coordinators (CC/separate sends) while still owning the review with one assignee, or make the assignee explicit/configurable per node — and update the submission flow plus `feasibility_reminder` (`applications/tasks.py:55`) to match. Also pin an `order_by` so the assignee choice is deterministic. Raised Low → Medium: it bit REDIB-2601-005 and feasibility starts within days of the October open. Default decision: notify all active node coordinators of the node; assignee = first by `pk`. (Source: 2026-05-04 chat — REDIB-2601-005 went only to Ángel even though CIC-biomaGUNE has a second active node coordinator.) |
| 34 | Medium | T3 inline | **Failed emails are logged and forgotten.** `send_email_from_template` swallows SMTP errors (returns False, `EmailLog` status `failed`) with no retry and no alert. On 2026-07-10 the `acceptance_reminder` to the applicant of REDIB-2601-026 failed (connection reset) — the last reminder before that applicant's deadline — and 2601-026 is the one that expired. 369 sent / 2 failed on the call, so it is rare but the misses land on deadlines. Minimal fix: Celery `autoretry_for` on transport errors with backoff (3 tries over ~1 h); coordinator visibility can be the existing `EmailLog` admin filtered to `failed` — a dashboard panel is not needed this round. (Source: REDIB-2601 call summary, 2026-08-18.) |
| 26 | Low | T3 inline | **`ConsultRequest.emails_sent_at` means "queued", not "delivered".** `calls/views.py` stamps it whenever `.delay()` didn't raise, but `send_email_from_template` swallows missing/inactive-template and SMTP errors (returns False), so the coordinator list's "Not emailed" badge never shows for requests whose notifications actually failed. Either check the eager return value / `EmailLog` status, or rename the badge to "not queued" and add an EmailLog-backed "delivery failed" indicator. Fold into 34 if that lands. (Source: 2026-08-18 review of PR #33.) |
| 37 | Medium | T3 inline | **All three app containers race to run `migrate` on every deploy.** `docker/entrypoint.sh` is shared by `web`, `celery`, and `celery-beat` (all three `build: .`) and runs `manage.py migrate --noinput` unconditionally; all three also gate on the same `depends_on: db: condition: service_healthy`, so the health gate releases them simultaneously. On the 2026-08-18 deploy of `calls/0003` two of the three crashed with `ProgrammingError: relation "calls_consultrequest" already exists` — one container committed the `CreateModel` while the others were mid-plan. Self-heals (`restart: unless-stopped` → retry finds "No migrations to apply"), and postgres DDL is transactional so the losers roll back cleanly; schema verified consistent afterwards. Latent since `b315393` (2026-02-10) but invisible until now because almost every migration since April is a choices-only `AlterField` that `sqlmigrate` renders as literal `-- (no-op)` (communications 0004-0007, calls 0002); `calls/0003` is the first `CreateModel` since `newsletters/0001` (applied 2026-06-02, logs long rotated — may well have raced unnoticed). Why it matters now: T2 (`eval-reminders`, `release-gate`, `resolution-report`) all add models/fields, so DDL-bearing deploys are about to become routine right before the October call, and a *real* migration failure is indistinguishable in the log from this benign one. Fix: wrap the migrate step in a postgres advisory lock (`pg_advisory_lock`) so the three serialize and the losers no-op — preferred over gating migrate to `web` only, which would let celery boot against the old schema. While there, check whether `seed_email_templates` (same entrypoint, also runs 3x concurrently) is idempotent under concurrency — not verified. **Triage 2026-08-18 (handoff session):** not `call-hardening` — wrong files (`docker/entrypoint.sh`, compose) and wrong review lens; it goes inline on `main`. Deadline: in prod before the `release-gate` deploy (~2026-12), the one migration this round that lands in a live call; ideally in the pre-open batch by 2026-10-13. Note it **cannot be verified in dev** — no Docker locally — so verification is the next real deploy. (Source: 2026-08-18 deploy of PR #32/#33.) |
| 38 | High | needs triage | **Call announcements fan out to every account with no throttle, no retry, and no `is_active` filter.** `notify_call_audience` (`calls/services.py:38-41`) queues one `send_email_from_template` task per row of `User.objects.filter(receive_call_notifications=True)`. That field defaults to `True` (`core/models.py:259`) and no opt-out has ever been exercised, so on prod today the audience is **1193 of 1193 accounts — including the 6 with `is_active=False`**, because the query never checks the account flag (same defect family as 19; it would have mailed `bioimac@ucm.es` after we deactivated it). Each announced call therefore pushes ~1200 real SMTP messages through `smtp.ionos.es:587` **twice** — once on Announce (`calls/views.py:471`) and again when the call auto-opens (`calls/services.py:97`) — from a worker at `--concurrency=2`, with no `rate_limit` on the task and no retry on failure (34), one `EmailLog` row each. IONOS shared SMTP enforces per-hour/per-day caps that are **not documented anywhere in this repo — find the actual ceiling first**; a burst that size can be throttled or rejected part-way through, leaving only a block of `failed` EmailLog rows and no alert, and repeated bursts put the `noreply@redib.net` sender reputation at risk. Before the ~09-15 announce: add `is_active=True` to the recipient query; rate-limit or batch the fan-out to the real ceiling; and put the recipient count on the Announce/Publish confirmation so the coordinator sees how many messages they are about to send. The October announce will be the largest email event the portal has ever run. (Source: 2026-08-18 prod investigation.) |
| 39 | High | needs triage | **Confirm the call-lifecycle email points — the "Now Open" send on auto-open is new and has never run on a real call.** Until `5e11104` (2026-08-17, deployed to prod 2026-08-18) a call had exactly one notification moment: a human pressed Publish. There are now two, and the second fires **with no human in the loop** — an `announced` call reaching `submission_start` auto-promotes and sends `call_published` to the whole audience, either from the daily beat task (`calls/tasks.py:12`) or, between beats, as a side effect of any anonymous page load of `/calls/` or a call detail page (`calls/views.py:75,107` → `open_announced_calls`, `calls/services.py:97`). Things to settle before the October call rather than after: a public GET triggering a ~1200-message send (38); the same audience being mailed twice a month apart with nothing tying the two sends together; `published_at` doing double duty as both "announced at" and "opened at" (`calls/views.py:466` and `:523`, papered over in `templates/calls/detail.html:107-109`); and no preview or dry-run anywhere in the path. Deliverable is a written contract — **which lifecycle transitions email whom, and who triggers them** across announce / open / close / resolve plus the `submission_end` nudges (35) — and then code that matches it. This is a confirm-the-design item as much as a code item; it needs to be settled before the ~09-15 announce, which is the first time the announce→auto-open pair runs for real. (Source: 2026-08-18 prod investigation.) |
| 40 | High | needs triage | **Decide whether call status is date-derived or coordinator-set — today it is both, and they disagree.** The `Call` lifecycle is driven from two independent mechanisms. **(a) Dates:** `check_call_deadlines` (`calls/tasks.py:12`) and the view-level fallbacks (`calls/views.py:39-64`, called on every public calls page hit) promote `announced → open` at `submission_start` and `open → closed` past `submission_end`; the public querysets then filter on dates *as well as* status (`calls/views.py:80-88`), so what an applicant sees depends on a status field and a date range that are free to contradict each other. **(b) Manual:** the coordinator sets `status` directly from the form dropdown (27) and from the Announce/Publish/Close actions, each with its own ad-hoc guards. The overlap produces states neither mechanism owns: `open` with a future start (in neither public list, live at its direct URL); `announced` with a past end (which is why `2ee0c7c` had to add a "never auto-open an expired call" guard to `open_announced_calls`); and a call whose dates are edited after announcement, where nothing re-evaluates the status at all (GitHub #23). Pick one model and subordinate the other: either **dates are truth** — status becomes derived (or a cache the tasks maintain), the dropdown goes away, and coordinator actions edit dates — or **status is truth** — transitions happen only through guarded actions, dates are metadata shown to applicants, and the auto-open/auto-close tasks either disappear or become reminders to the coordinator instead of silent writes. **Almost certainly too much rework to land before the October call — what is due now is the assessment, not the implementation.** Write up both models with their cost, note which of 27 and 39 collapse into each, and list which existing rows would change behavior; then decide in round planning whether any slice is safe this round. Captured while the evidence is fresh. (Source: 2026-08-18 prod investigation.) |

## Close-out of a running call (execution phase)

| # | Priority | Round | Item |
|---|----------|-------|------|
| 29 | High | closeout | **Execution-phase completion reminders: log actual hours and mark the project done.** Nothing in the beat schedule nudges anyone to record `actual_hours_used` or complete an application; the only post-handoff task is `send_publication_followups` (6 months after handoff, publications only). Result for REDIB-2601 (execution 07-01 → 10-30): 0 of 36 equipment lines have `actual_hours_used`, 0 completions, and the network has no idea which projects are finished. Add a beat task that, for every application with `status='accepted'`, `accepted_by_applicant=True`, `handoff_email_sent_at` set and `is_completed=False`, emails **both the applicant (`applicant_email` helper) and the node coordinator(s) of the equipment's node(s)** reminding them that when the work is finished they must mark each equipment line done and enter the final hours used in the portal (link to the application detail, where `mark_equipment_done` / `node_confirm_equipment_done` already live). **Default cadence (adjust here if the network wants otherwise):** first reminder 60 days after `handoff_email_sent_at`, then every 30 days, plus one on the day after `call.execution_end`; stop when `is_completed=True`. Two DB email templates (applicant / node coordinator), seeded via `seed_email_templates`; log to `EmailLog` like the other reminders. Also usable immediately for the summer call: the first run will catch all 22 active REDIB-2601 projects. (Source: 2026-08-18 round-planning chat + REDIB-2601 call summary item K.) |
| 30 | High | closeout | **Waitlist follow-up: remind node coordinators to promote or close out waitlisted applications, and close the loop when capacity frees.** After an applicant accepts a waitlist offer (`status='pending'`, `accepted_by_applicant=True`) the application has no further lifecycle: no reminder, no closure, no "not reached this call" outcome. All 7 REDIB-2601 waitlisted applicants accepted on 06-26/30 and have heard nothing since; when 2601-026 expired on 07-11 it freed 54 h at BioImaC but `acceptance_expired` went only to the applicant, so no coordinator learned a slot opened. Three parts, in order: **(a)** beat task that emails the node coordinator(s) a per-node digest of their accepted-waitlist applications asking "have any of these been promoted to active? if so, click *Mark as Accepted* on Access Tracking; if the slot will not open this call, close them out" — **default cadence:** first at 30 days after the applicant's acceptance, then every 30 days, plus one on the day after `call.execution_end`; **(b)** a node-coordinator/coordinator **"Not reached this call" close-out action** on Access Tracking that ends the waitlist state (new terminal outcome, reason field, applicant email template `waitlist_not_reached`) so waitlisted rows do not dangle forever — needed to actually close REDIB-2601; **(c)** when an accepted application expires or is declined, also notify the node coordinator(s) of the freed equipment/hours so they can promote from the waitlist. Depends on 31 (promotion must set hours) and pairs with 17 (waitlist acceptance deadlines). (Source: 2026-08-18 round-planning chat + REDIB-2601 call summary item B.) |
| 36 | Medium | closeout | **Call close-out: move a call to `resolved` and lock resolution from the UI.** `check_call_deadlines` auto-closes a call at `submission_end` but nothing ever moves it `closed → resolved` or sets `is_resolution_locked`, which is why REDIB-2601 is still `closed`. The only existing lock path is `applications:finalize_resolution` (`applications/services/resolution.py:finalize_resolution`), the legacy ReDIB-coordinator bulk flow that also re-dispatches resolution notifications — not safe to press on a call whose node resolutions already went out. Add a small coordinator action on call detail: "Mark call resolved" → sets `status='resolved'` + `is_resolution_locked=True`, guarded by "no application still in `evaluated`", with no email side-effects; this is also the natural "resolution complete" flag that 20/21 need. Document the step in the operations handbook alongside the resolution-table publication. (Source: REDIB-2601 call summary item K, 2026-08-18.) |

## Workflow features

| # | Priority | Round | Item |
|---|----------|-------|------|
| 32 | High | eval-reminders | **Evaluation reminders: per-evaluator digest on a backoff schedule.** `send_evaluation_reminders` (`evaluations/tasks.py:16`) sends one email per pending evaluation every day with no digest and no dedupe. On REDIB-2601 two evaluators got 42 reminders each (6 apps × 7 days) and a third got 29 — 113 of the 130 reminder emails went to three people. Real risk they filter portal mail before the next call. Replace with one email per evaluator listing all their pending evaluations, sent at −7 / −3 / −1 days before `evaluation_deadline` and then daily-or-every-other-day once overdue (keep `notify_overdue_evaluators` / `notify_coordinator_overdue_evaluations` semantics, just fold the per-evaluation sends into one message). Existing template can be reworked to accept a list. Must land before evaluations open on the new call. (Source: REDIB-2601 call summary item C, 2026-08-18.) |
| 15 | High | release-gate | **Gate evaluated applications behind a ReDIB-coordinator release, then batch node resolution.** Today, once an application's assigned evaluations are all submitted it independently transitions `under_evaluation` → `evaluated` and immediately becomes actionable: it is pushed to the node coordinator, who can accept it, which bounces it to the applicant, who accepts, which makes it active — all asynchronously, one application at a time as each finishes evaluating. This biases node decisions: a node coordinator resolves applications piecemeal instead of weighing the whole pool of approved applications against limited capacity (REDIB-2601 resolutions spread 06-05 → 06-26). Applications should instead be considered **all at once, after every evaluation score for the call is in**, so the node can unbiasedly sort the full set into accepted / waitlisted / rejected. (It is the **node coordinator** who makes the call per application — not the ReDIB coordinator and not an automatic rule.) **Minimal design for this round (no new application state):** a per-call `resolutions_released` flag + coordinator "Release to nodes" action on call detail; while unset, the `evaluated` transition still happens but the node-coordinator notification email is held and the node resolution queue/review views refuse to act; on release, send the node emails for the whole batch. Touches: the `evaluated` transition hook, `applications/services/node_resolution.py`, node-resolution views/templates, call detail. **Report note (I):** the two evaluators disagreed by ≥5 of 12 points on 12 of 24 REDIB-2601 applications (12 vs 4, 11 vs 3, 7/approved vs 0/denied) — surface the score spread per application on the release screen; whether to add a third evaluator or a documented tiebreak is a network policy decision, not a portal change this round. Needed before evaluations complete on the new call. (Source: 2026-06-08 chat; call summary 2026-08-18.) |
| 20 | High | resolution-report | **Coordinator report: bilingual per-call resolution table (EN + ES) for publication.** Once every resolution for a call is in, ReDIB must publish a results table listing each submitted application, its applicant organization, the node it was submitted to, and the node coordinator's resolution — in **both English and Spanish**, as two separate tables. There is no way to produce this from the portal today; on 2026-07-23 it was generated by hand from the prod DB via `manage.py shell`. Build it as a report on the coordinator dashboard (`reports/`), with per-call selection, both language variants, and export (at minimum copy-paste-able HTML/markdown; CSV/XLSX desirable). **Recipe used manually (encode this in the report):** (1) the call is `Call.objects.get(code='REDIB-2601')` — note the DB code is `REDIB-2601`, not the `ReDIB2601` form people say out loud; (2) rows = `Application.objects.filter(call=c).exclude(status='draft')` ordered by `code` (for REDIB-2601: 27 applications, 3 still drafts → 24 rows); (3) the resolution column comes from **`NodeResolution.resolution`** (the node coordinator's decision: `accept` / `waitlist` / `reject`), *not* `Application.status` or `Application.resolution` — the published resolution is independent of whether the applicant later accepted, so REDIB-2601-026 publishes as *Accepted* despite having gone `expired`; every REDIB-2601 application requested equipment at exactly one node, so one resolution per row, but the report must handle multi-node applications (one row per node, or a combined cell — decide); (4) label maps — EN `accept→Accepted`, `waitlist→Wait List`, `reject→Rejected`; ES `accept→Aceptada`, `waitlist→En espera`, `reject→Rechazada`; (5) organization cell = `application.applicant.organization` rendered `short_name · name` with the `·` separator, falling back to `name` alone when `short_name` is blank or identical to `name` (4 of 24 rows on this call: Universidad de Navarra, Hospital Universitario La Paz, Hospital Universitario de Guadalajara, Imaging La Fe) — note this is the *user profile's* organization, not the free-text `Application.applicant_entity`; (6) node cell uses the public display names **BioImaC / biomaGUNE / TRIMA / IIS La Fe**, mapped from `Node.code` (`BioImaC`, `CIC-biomaGUNE`, `TRIMA@CNIC`, `IIS-LaFe`) — this mapping doesn't exist anywhere in code yet and needs a home. REDIB-2601 result for regression-checking the report: 16 accepted, 7 wait list, 1 rejected. Machine-readable output for 21 is *not* required this round. **Note for Ryan:** this procedure also has to go into the **ReDIB Operations Handbook** — publishing the resolution tables after every call is a network operating procedure, not just a portal feature. (Source: 2026-07-23 chat.) |
| 16 | Medium | release-gate (stretch) | **Distinguish "accepted by node, awaiting applicant" from "accepted by applicant".** There are effectively two acceptance steps — the node coordinator accepts an application (`status='accepted'`), then the applicant must accept within their 10-day window (`accepted_by_applicant=True`) — but both currently surface identically as "accepted" in every status badge and coordinator view. There is no way, from the status or any coordinator-facing list, to see that an applicant still hasn't accepted (e.g. REDIB-2601-023 and -027 sat node-accepted but applicant-unaccepted while -005/-011/-016/-025 were fully accepted, all indistinguishable). Surface the distinction without adding a workflow phase: the data already exists on `Application` (`accepted_by_applicant`, `accepted_at`, `acceptance_deadline`, `handoff_email_sent_at`). Preferred: a derived sub-state badge ("Accepted — awaiting applicant" vs "Accepted — confirmed") computed from those fields, with the acceptance deadline shown for the pending ones, plus a filter so coordinators can pull "node-accepted but not yet applicant-confirmed". Affected: `templates/includes/status_badge.html`, the coordinator/Access Tracking views and call-detail status wall (`calls/views.py`, `applications/views.py`). Same ask as GitHub #27. (Source: 2026-06-22 chat.) |
| 5 | Medium | eval-reminders | **One-button reminder dispatch for outstanding work.** The daily Celery tasks (`send_feasibility_reminders`, `send_evaluation_reminders`, `notify_overdue_evaluators`, `notify_coordinator_overdue_evaluations`) handle automatic reminders well. But the ReDIB coordinator should also have an on-demand button to fire reminders for a chosen scope ("remind all open feasibility reviews now", "remind all evaluators with un-submitted scores"). Useful when a deadline is approaching off-cycle or after manual data updates. Likely a new coordinator dashboard panel that calls the existing reminder helpers in `applications/tasks.py` and `evaluations/tasks.py` synchronously per click. Build on top of 32's digest helper so the manual button and the scheduled task send the same email. (Source: 2026-04-16 chat.) |
| 35 | Medium | closeout (stretch) | **Draft nudge before the submission deadline.** 3 REDIB-2601 applications never left `draft` and received zero emails; two went quiet ~3 weeks before close. Add a beat task that emails applicants with a `draft` on an open call at T−7 and T−2 days before `submission_end` ("your application is still a draft — submit before …"). One template, one task, `EmailLog` dedupe per (application, offset). Cheap conversion win for the October call. (Source: REDIB-2601 call summary item J, 2026-08-18.) |
| 18 | Medium | T3 inline | **Coordinator "reinstate expired application" action.** `expired` is a terminal state in `Application.VALID_TRANSITIONS` (`applications/models.py:308`, `'expired': []`), and `Application.save()` raises `ValidationError` on any transition out of it — there is no supported UI or admin path to reverse an expiry when an applicant lets the 10-day window lapse but the work legitimately needs to proceed (e.g. a PI/node started access anyway). Today this requires shell surgery: temporarily monkeypatching the in-memory transition table, then setting `status='accepted'`, `accepted_by_applicant=True`, `accepted_at`, and `handoff_email_sent_at` (+ optionally re-sending `handoff_notification`) on a real `.save()` so simple-history records it. Build a proper ReDIB-coordinator action (button on Access Tracking / call-detail) that reinstates an `expired` (and probably `declined_by_applicant`) application to the "Active" end-state, with a **mandatory reason field** written to `history_change_reason` and the acting user captured via `_history_user`, and a choice of whether to re-fire the handoff email. Allow the reverse transition through a dedicated service method rather than relaxing `VALID_TRANSITIONS` globally, so the terminal-state guard stays strict for the normal flow. **Done manually 2026-07-07:** reinstated REDIB-2601-015 to Active via shell (auto-expired 07-04 after the PI at CNIC started work without accepting). **Deferred extension (report item E, not this round):** REDIB-2601-020 was submitted to the wrong node, feasibility-rejected (terminal), bounced to `draft` by the right node, then walked `submitted → under_evaluation` by shell, leaving its `FeasibilityReview` at `edits_requested` / `is_feasible=None`; a real "re-route to another node" action from `rejected_feasibility` would cover that, but it is a 1-in-24 edge case — shell workaround stands. (Source: 2026-07-07 chat; call summary 2026-08-18.) |
| 4 | Medium | — | **Auto-assign evaluators: confirm-and-review step before emails go out.** Today the ReDIB coordinator clicks the per-call auto-assign button and the system immediately creates `Evaluation` rows AND sends assignment emails. The assignments can be edited after, but the emails are already out — hard to walk back. Add a two-step flow: (1) auto-assign computes a *preview* (which evaluators would be assigned to which apps, with COI/area annotations); (2) coordinator reviews and tweaks; (3) coordinator clicks "Confirm and notify" which persists the assignments and triggers the emails. Affected: `evaluations/views.py:auto_assign_call`, `evaluations/tasks.py:assign_evaluators_to_call`, plus a new preview template. The confirm step should also unify the auto-assign / manual-assign / combo workflows (a bug where manual assignment didn't transition `pending_evaluation` → `under_evaluation` was fixed in `07468fc`; the confirm step should make that structurally impossible). **Report note (H):** show per-evaluator load on the preview — REDIB-2601 had 7 evaluators at 6 apps each and the other 3 at 3/2/1. **Deferred:** it reworks the assignment flow, and the one-shot flow worked last call. (Source: 2026-04-16 chat + 2026-04-17 livetest; call summary 2026-08-18.) |
| 12 | Medium | — | **Dedicated `edits_requested` application status.** When a feasibility reviewer requests edits, the application is currently shoved back into `status='draft'` and `submitted_at` is cleared (`applications/views.py:761-768`), reusing the never-submitted draft state. This caused REDIB-2601-024 to silently disappear from the coordinator status wall on `/calls/<pk>/detail/` (2026-05-19). A quick fix keeps bounced drafts visible by detecting the presence of any `FeasibilityReview` row (`calls/views.py:call_detail`); the proper fix is a distinct workflow state (e.g. `edits_requested`) in `APPLICATION_STATUSES` and updates to the bounce path, templates / status badges / role-aware filters in `applications/views.py`, `calls/views.py`, and any reports that filter on `draft`. **Report note (F):** the new state should carry its own per-application resubmission deadline — REDIB-2601-020 re-entered 20 days after `submission_end`, was evaluated at the deadline and resolved last. **Deferred:** new enum state across views/templates/reports is structural; the quick fix holds. (Source: 2026-05-19 chat; call summary 2026-08-18.) |
| 21 | Medium | — | **Public "past calls" archive with published resolution tables.** Applicants and the wider network have no way to see previous calls once they close — the `/calls/` page is oriented to what's currently open/upcoming. Add an archive of closed calls (an "Open / Past" split or a per-call public detail view on the existing calls app) where each past call shows the same details it displayed when it was posted plus the resolution table from item 20 for any call whose resolution is complete (item 36's `resolved` flag), in both English and Spanish. Design points to settle: live render vs frozen snapshot of `Call`/`Equipment` rows (a snapshot is likely more honest for a published record); public visibility and what applicant-identifying information is safe (organization-level, not person-level, by design); stable permanent URLs so the tables can be linked from redib.net and from emails. Depends on 20 and 36. **Lowered High → Medium and deferred:** publication can keep going via the manual/20 export until the marketing cutover decides where public pages live. (Source: 2026-07-23 chat.) |
| 19 | Medium | — | **Reassess `User.is_active` vs `UserRole.is_active` — one concept or two?** Every role lookup in the app filters `UserRole.is_active=True`, but only two call sites also check the account itself (`evaluations/tasks.py:418` and `evaluations/views.py:210-215`, both patched after an evaluator opted out for a session). The remaining ~20 role queries (`applications/views.py` ×11, `access/views.py` ×3, `evaluations/tasks.py:644`, `applications/services/node_resolution.py:169`, `core/views.py:54`, `core/forms.py:149`, `core/decorators.py:29`) look at the role flag only, so a deactivated **account** with a live role still receives task emails it can never act on — it just can't log in. Hit again on 2026-07-23 deactivating `bioimac@ucm.es` (BioImaC): both flags had to be set by hand, and `populate_redib_users.py:257-264` hardcodes `is_active: True` on every role it writes, so the TSV's `is_active` column can never *deactivate* a role — the row's `roles` cell had to be blanked. Decide the model and enforce it once: either (a) collapse to a single source of truth, or (b) keep both with a crisp definition and route **all** lookups through one helper (`UserRole.objects.active()` / `User.active_roles()`), plus a loader that can express role deactivation. Ban raw `.filter(role=…, is_active=True)` in app code. Related: 13. **Deferred:** cross-cutting refactor; the two-flag manual procedure is documented and works. (Source: 2026-07-23 chat + earlier evaluator opt-out.) |
| 10 | Blocked | — | **Proxy applicants: applications filled out on behalf of another PI.** Currently the application is rigidly tied to the logged-in user as the sole responsible applicant — consistent with the PDF flow and required for the legal-style checkboxes (prior contact with node, consent to share personal info, etc.). A request has come in to allow alternate names / contact / organization in the application itself. To do this cleanly we'd need a processor-vs-applicant model with separate sign-offs (analogous to legal proxy forms), which adds fields and steps not in the PDF. **Do not implement** until the network agrees on a course of action. When/if approved: design the data model so the official applicant is still a registered `User` (preserving FK chain to organization, etc.) with a distinct "submitted by" processor identity and dual sign-off fields. (Source: 2026-05-05 email to nodes.) |
| 11 | Blocked | — | **Additional application contacts (CC list) with opt-out.** Request to allow applications to carry a list of extra contact emails that get CC'ed on application correspondence. Not implemented originally because (a) we rely on email verification to confirm the recipient consents to be emailed, and (b) sending to arbitrary user-supplied addresses without an unsubscribe path edges into spam territory. If approved by the network, the feature needs an opt-out / unsubscribe mechanism for CC'd addresses and a clear scope ("contacted only during the application lifecycle"). **Do not implement** until the network agrees. Until then, nodes can continue using their existing direct-email channels to loop in collaborators. (Source: 2026-05-05 email to nodes.) |

## UX polish

| # | Priority | Round | Item |
|---|----------|-------|------|
| 23 | Low | T3 inline | **Scientific-project guidance text: worked examples per field (blocked on Ángel).** The Batch 1 requirements doc (`~/tmp/protal_issues_20260401.md`, 2026-04-01) deferred issue #25: the applicant-facing guidance/placeholder text on the Step 1/3 scientific fields (`applications/forms.py` help_texts and placeholders, `templates/applications/wizard_step1.html`, `wizard_step3.html`) was to be improved with concrete example text once Ángel supplied approved examples. Nothing has arrived and the text is unchanged; leave as-is until examples exist, then it is a copy-only change (no model/migration) — worth a nudge to Ángel before the October open. (Source: 2026-08-17 review of the April requirements doc; every other item in it shipped in `fixes-batch-1`, merged `62d9180`.) |
| 1 | Low | — | **Equipment details drill-in on calls listing.** On `/calls/`, equipment is listed by name only with no way to view details. A click or hover affordance to surface equipment specs/availability/photos would help applicants assess fit before opening a full application. (Source: localtest3 walkthrough P1.) |
| 2 | Low | — | **Role-relative status coloring.** Today the status badges on application lists use the same colors regardless of who is viewing. Red could be reserved for "this viewer needs to act on this app" rather than a globally "bad" state — so an applicant sees red on apps awaiting their response, an evaluator sees red on assignments they owe, etc. Requires per-role rendering of the shared `templates/includes/status_badge.html`. Same family as GitHub #20/#27. (Source: localtest3 walkthrough P2.) |
| 3 | Low | — | **Organization picker: search/better sort in the profile form.** The organization dropdown on `/profile/` has many options and is hard to scan. Add a typeahead/autocomplete (e.g., select2 or similar lightweight HTMX widget), or at minimum sort by frequency-of-use. (Source: 2026-04-16 chat.) |

## Tooling / data ingestion

| # | Priority | Round | Item |
|---|----------|-------|------|
| 13 | Medium | call-hardening (minimal) | **`populate_redib_users` sync semantics.** The current loader (`core/management/commands/populate_redib_users.py`) is unsafe to re-run on prod for two reasons. (1) It unconditionally `user.set_password('changeme123')` on every row (lines 189-190), so any user who has set their own password loses it. (2) It uses `update_or_create` with TSV values winning unconditionally — so any field that drifted from the TSV after first load (a coordinator edited their phone in `/profile/`, or `is_active` was toggled via the shell) silently gets stomped on the next reload. **This round, minimal slice only:** set the password only for *newly created* users, and add `--dry-run` that prints the create/update diff and exits — because new evaluators/coordinators for the October call will be loaded from the TSV. The full conflict-resolution policy (who wins per field, TSV-snapshot diffing) stays deferred; until then do targeted DB updates by hand for one-off ops changes. (Source: 2026-05-20 chat — activating bjrubio@ucm.es from the new TSV without running the loader.) |
| 6 | Medium | — | **xlsx → tsv conversion + QC tooling.** The reference TSVs in `data/` are produced by exporting from Microsoft Excel on Windows, which writes Windows-1252 (CP1252) text and CRLF line endings. Our loaders assume UTF-8, so non-ASCII names (e.g. `Asociación`, `Compostela`) crash the import on the first row. Today the workaround is a manual `iconv -f cp1252 -t utf-8` pass plus eyeballing column counts. Build a small `manage.py` command (or standalone script) that takes the source `.xlsx`, writes UTF-8 TSV, and runs file-specific QC. Checks observed in real data so far: encoding (force UTF-8 output); trailing-tab artifacts in headers; leading/trailing whitespace in cells; underscore-style phone-extension suffixes (e.g. `+34 91 ... _5404`) and Excel treating a leading `+` as a formula; typos in enum-valued columns (e.g. `prelcincal` for `preclinical`, `Public Research Organisation (PRO)` exact-spelling check); separator drift (`,` used where `;` is required, e.g. `users.tsv` `areas`); ambiguous booleans (blank vs explicit TRUE/FALSE); FK lookups (`organization_name` → `organizations.tsv:name`; `roles` `node_coordinator:CODE` → `nodes.tsv:code`; `equipment.tsv` `node_code` → `nodes.tsv:code`); duplicate natural keys; required columns present. Runnable per-file (`--file organizations`) and as a batch. (Source: 2026-04-16 chat / real-data prep.) |
| 22 | Low | — | **Newsletter image handling — replace the manual base64 inliner.** The `newsletters` app serves the uploaded HTML file raw in an iframe and the `Newsletter.html_file` field requires a *self-contained* file ("inline CSS, no external assets"), so the only way to include images today is to base64-embed them as data URIs inside the single HTML file. For the June 2026 issue this was done with a throwaway build script (`/home/rtasseff/tmp/redibNewsletter/build_newsletter.py`): it downscales/compresses each PNG with ImageMagick and inlines them, producing a ~1.5 MB self-contained HTML. Options: (1) a `NewsletterImage` inline model in the admin (upload images alongside the newsletter, reference by a stable `/media/...` URL — the raw iframe serves at `/newsletters/<slug>/raw/`, so images must use root-relative or absolute URLs); (2) an upload-time processing step that accepts a folder/zip and auto-inlines + optimizes (productionize the throwaway script as a `manage.py` command); (3) a richer authoring model rendered server-side. Keep the iframe `X-Frame-Options: SAMEORIGIN` serving intact and confirm images load in both dev and prod (Caddy serves `/media/`). **Lowered Medium → Low:** not call-related; the script works. (Source: 2026-06-01 chat — first newsletter, Option A chosen as the interim.) |

## Test infrastructure

| # | Priority | Round | Item |
|---|----------|-------|------|
| 7 | Low | baseline | **8 tests blocked by `ProfileCompletionMiddleware`.** The middleware (`core/middleware.py`, added in batch-1) redirects logged-in users with incomplete profiles to `/profile/`. Several tests in `tests/test_design.py`, `tests/test_phase7_acceptance.py`, `tests/test_phase9_publications.py`, and `reports/tests.py` create users via `User.objects.create_user(username, email, password)` only — no `first_name`, `last_name`, `phone`, `organization`, or `position` — and so get redirected when they try to GET/POST a protected view. Fix: add a shared `make_complete_user()` helper (e.g., in `tests/__init__.py`) that fills the required fields, and update the affected `setUp` methods. Cheap; improves the review baseline for every bucket this round. (Source: 2026-04-16 chat / pre-merge review.) |
| 8 | Low | baseline | **3 tests blocked by `ManifestStaticFilesStorage`.** `tests/test_design.AuthPageRenderTest.{test_login,test_logout,test_signup}_page_renders` render templates that include `{% static 'images/Logo-ICTS-def-low.jpg' %}`. The configured storage backend requires a built `staticfiles.json` manifest, which the test environment doesn't produce. Easiest fix: override `STORAGES['staticfiles']['BACKEND']` to `django.contrib.staticfiles.storage.StaticFilesStorage` in test settings (or in those test classes via `@override_settings`). (Source: 2026-04-16 chat / pre-merge review.) |

---

## Side note — GitHub issues

`gh issue list` still carries #14–#30 from the April 2026 pre-launch test
period (imported from the SharePoint list). Most predate `fixes-batch-1`
and the summer call; several duplicate rows here (#27 ≈ 16, #20 ≈ 2, #19 =
shipped waitlist flow, #23 ↔ 27). They are **not** part of this round's
plan — triage and close them in a separate pass, moving anything still
real into this file.
