# Developer Backlog

Running list of feature requests, UX polish, and known issues to address in
future batches. **This file is dynamic** — add new items as they come up,
mark items done (or remove) when they ship in a batch.

## How to use this file

- **Add new entries** to the bottom of the relevant section. Keep each
  entry to a short title plus a one-paragraph description (what + why).
- **Tag** with priority (`Low` / `Medium` / `High`) and category in the
  table column.
- **Cross-link** to source when applicable: a test-log entry, a chat
  conversation, an issue number.
- **When picked up**: move the entry into a batch implementation plan
  (`docs/developer/batchN-implementation-plan.md`) and either delete it
  here or strike it through with a pointer to the batch. Keep the file
  short — this is a backlog, not an archive.

For comparison: `developer-notes.md` records *design decisions already
made* (with a "what to do next when ready" section). This file records
*work not yet started*.

---

## In-flight branches (not on `main`)

Registry of active worktrees (dir, port, status): [worktrees.md](worktrees.md).

---

## UX polish

| # | Priority | Item |
|---|----------|------|
| 1 | Low | **Equipment details drill-in on calls listing.** On `/calls/`, equipment is listed by name only with no way to view details. A click or hover affordance to surface equipment specs/availability/photos would help applicants assess fit before opening a full application. (Source: localtest3 walkthrough P1.) |
| 2 | Low | **Role-relative status coloring.** Today the status badges on application lists use the same colors regardless of who is viewing. Red could be reserved for "this viewer needs to act on this app" rather than a globally "bad" state — so an applicant sees red on apps awaiting their response, an evaluator sees red on assignments they owe, etc. Requires per-role rendering of the shared `templates/includes/status_badge.html`. (Source: localtest3 walkthrough P2.) |
| 3 | Low | **Organization picker: search/better sort in the profile form.** The organization dropdown on `/profile/` has many options and is hard to scan. Add a typeahead/autocomplete (e.g., select2 or similar lightweight HTMX widget), or at minimum sort by frequency-of-use. (Source: 2026-04-16 chat.) |

## Features

| # | Priority | Item |
|---|----------|------|
| 4 | Medium | **Auto-assign evaluators: confirm-and-review step before emails go out.** Today the ReDIB coordinator clicks the per-call auto-assign button and the system immediately creates `Evaluation` rows AND sends assignment emails. The assignments can be edited after, but the emails are already out — hard to walk back. Add a two-step flow: (1) auto-assign computes a *preview* (which evaluators would be assigned to which apps, with COI/area annotations); (2) coordinator reviews and tweaks; (3) coordinator clicks "Confirm and notify" which persists the assignments and triggers the emails. Affected: `evaluations/views.py:auto_assign_call`, `evaluations/tasks.py:assign_evaluators_to_call`, plus a new preview template. **Note (2026-04-17):** The confirm step should also unify the auto-assign / manual-assign / combo workflows. A coordinator may auto-assign most evaluators and then manually add or swap a few — or skip auto-assign entirely and hand-pick all evaluators. The confirm-and-review screen should work for all three paths: show the proposed roster (however it was built), let the coordinator finalize, and only then persist assignments + fire emails. A bug where manual assignment didn't transition `pending_evaluation` → `under_evaluation` was fixed in commit `07468fc`; the confirm step should make this kind of state-sync issue structurally impossible. (Source: 2026-04-16 chat + 2026-04-17 livetest.) |
| 5 | Medium | **One-button reminder dispatch for outstanding work.** The daily Celery tasks (`send_feasibility_reminders`, `send_evaluation_reminders`, `notify_overdue_evaluators`, `notify_coordinator_overdue_evaluations`) handle automatic reminders well. But the ReDIB coordinator should also have an on-demand button to fire reminders for a chosen scope ("remind all open feasibility reviews now", "remind all evaluators with un-submitted scores"). Useful when a deadline is approaching off-cycle or after manual data updates. Likely a new coordinator dashboard panel that calls the existing reminder helpers in `applications/tasks.py` and `evaluations/tasks.py` synchronously per click. (Source: 2026-04-16 chat.) |
| 9 | Low | **Fan out feasibility-request emails to all node coordinators.** When an application is submitted, `applications/views.py:585-597` looks up node coordinators with `.first()` and assigns the `FeasibilityReview` to a single (essentially arbitrary) user, who is then the only one emailed (`feasibility_request` at line 631). If a node has multiple active `node_coordinator` roles, the others are silent. Decide the intended behavior — notify all coordinators (CC/separate sends) while still owning the review with one assignee, or make the assignee explicit/configurable per node — and update the submission flow plus `feasibility_reminder` (`applications/tasks.py:55`) to match. Also pin an `order_by` so the assignee choice is deterministic. (Source: 2026-05-04 chat — REDIB-2601-005 went only to Ángel even though CIC-biomaGUNE has a second active node coordinator.) |
| 10 | Blocked — awaiting network decision | **Proxy applicants: applications filled out on behalf of another PI.** Currently the application is rigidly tied to the logged-in user as the sole responsible applicant — consistent with the PDF flow and required for the legal-style checkboxes (prior contact with node, consent to share personal info, etc.). A request has come in to allow alternate names / contact / organization in the application itself. To do this cleanly we'd need a processor-vs-applicant model with separate sign-offs (analogous to legal proxy forms), which adds fields and steps not in the PDF. **Do not implement** until the network agrees on a course of action. When/if approved: design the data model so the official applicant is still a registered `User` (preserving FK chain to organization, etc.) with a distinct "submitted by" processor identity and dual sign-off fields. (Source: 2026-05-05 email to nodes.) |
| 11 | Blocked — awaiting network decision | **Additional application contacts (CC list) with opt-out.** Request to allow applications to carry a list of extra contact emails that get CC'ed on application correspondence. Not implemented originally because (a) we rely on email verification to confirm the recipient consents to be emailed, and (b) sending to arbitrary user-supplied addresses without an unsubscribe path edges into spam territory. If approved by the network, the feature needs an opt-out / unsubscribe mechanism for CC'd addresses and a clear scope ("contacted only during the application lifecycle"). **Do not implement** until the network agrees. Until then, nodes can continue using their existing direct-email channels to loop in collaborators. (Source: 2026-05-05 email to nodes.) |
| 12 | Medium | **Dedicated `edits_requested` application status.** When a feasibility reviewer requests edits, the application is currently shoved back into `status='draft'` and `submitted_at` is cleared (`applications/views.py:761-768`), reusing the never-submitted draft state. This caused REDIB-2601-024 to silently disappear from the coordinator status wall on `/calls/<pk>/detail/` (2026-05-19). A quick fix was applied to keep bounced drafts visible by detecting the presence of any `FeasibilityReview` row (`calls/views.py:call_detail`), but the proper fix is to introduce a distinct workflow state (e.g. `edits_requested`) in the `APPLICATION_STATUSES` enum and update the bounce path, plus templates / status badges / role-aware filters in `applications/views.py`, `calls/views.py`, and any reports that filter on `draft`. Benefit: clearer signal to coordinators (own badge, deadline, possible reminder hook), no reliance on the "has a feasibility review?" heuristic, easier to wire SLA tracking on stalled revisions. (Source: 2026-05-19 chat.) |
| 16 | Medium | **Distinguish "accepted by node, awaiting applicant" from "accepted by applicant".** There are effectively two acceptance steps — the node coordinator accepts an application (`status='accepted'`), then the applicant must accept within their 10-day window (`accepted_by_applicant=True`) — but both currently surface identically as "accepted" in every status badge and coordinator view. There is no way, from the status or any coordinator-facing list, to see that an applicant still hasn't accepted (e.g. REDIB-2601-023 and -027 sat node-accepted but applicant-unaccepted while -005/-011/-016/-025 were fully accepted, all indistinguishable). Surface the distinction without adding a full workflow phase: the data already exists on `Application` (`accepted_by_applicant`, `accepted_at`, `acceptance_deadline`, `handoff_email_sent_at`). Options to evaluate — a derived sub-state badge ("Accepted — awaiting applicant" vs "Accepted — confirmed") computed from those fields, or a lightweight `applicant_accepted` flag column in the relevant lists, with the acceptance deadline shown for the pending ones. Affected: `templates/includes/status_badge.html` and any per-role rendering, the coordinator/Access Tracking views and call-detail status wall (`calls/views.py`, `applications/views.py`), and possibly a filter so coordinators can pull "node-accepted but not yet applicant-confirmed". (Source: 2026-06-22 chat.) |
| 15 | High (before next call) | **Gate evaluated applications behind a ReDIB-coordinator release, then batch node resolution.** Today, once an application's assigned evaluations are all submitted it independently transitions `under_evaluation` → `evaluated` and immediately becomes actionable: it is pushed to the node coordinator, who can accept it, which bounces it to the applicant, who accepts, which makes it active — all asynchronously, one application at a time as each finishes evaluating. This biases node decisions: a node coordinator resolves applications piecemeal instead of weighing the whole pool of approved applications against limited capacity. Applications should instead be considered **all at once, after every evaluation score for the call is in**, so the node can unbiasedly sort the full set into accepted / waitlisted / rejected. (Note for the record: it is the **node coordinator** who technically makes the call on how each applicant is treated for assignment based on the evaluations — not the ReDIB coordinator and not an automatic rule.) Desired system: keep evaluated applications in a held/pending state until the ReDIB coordinator explicitly **releases** them back to the nodes for assignment — i.e. a per-call gate (all evaluations complete → ReDIB coordinator reviews/releases → node coordinators do batch resolution). Affected areas to design around: the `evaluated` → `accepted`/`pending`/`rejected` transition and the resolution services (`applications/services/node_resolution.py`, `applications/services/resolution.py`), the node-coordinator resolution views/forms, and likely a new release action + state (or a per-call "released for assignment" flag) plus the Access Tracking / call-detail UI. **Letting it slide for the current call (REDIB-2601)** since resolution is already happening; required before the next call. (Source: 2026-06-08 chat.) |
| 17 | Medium | **Acceptance deadline task ignores waitlisted applicants.** `process_acceptance_deadlines` (`applications/tasks.py:240`) filters `status='accepted'` only in **both** branches — the day-7 reminder (`:263`) and the day-10 auto-expire (`:315`). But `pending` (waitlist) applications carry the same `acceptance_deadline` + `accepted_by_applicant` accept/decline window and surface in the UI as "Waitlist — Awaiting Applicant" (`templates/includes/status_badge.html:27`). Result: waitlisted applicants get **no reminder email and never auto-expire** — they sit at their deadline until a coordinator acts by hand. Fix: change both filters to `status__in=['accepted', 'pending']` (and sanity-check that the reminder/expire context and any downstream waitlist-promotion logic still read correctly for `pending`). Until shipped, send waitlist reminders manually via `send_email_from_template('acceptance_reminder', …)`. **Mitigation already done (2026-06-30):** manually sent `acceptance_reminder` to the 3 waitlisted applicants in call 1 (REDIB-2601-012 / -020 / -022). (Source: 2026-06-30 chat.) |
| 18 | Medium | **Coordinator "reinstate expired application" action.** `expired` is a terminal state in `Application.VALID_TRANSITIONS` (`applications/models.py:308`, `'expired': []`), and `Application.save()` raises `ValidationError` on any transition out of it — there is no supported UI or admin path to reverse an expiry when an applicant lets the 10-day window lapse but the work legitimately needs to proceed (e.g. a PI/node started access anyway). Today this requires shell surgery: temporarily monkeypatching the in-memory transition table, then setting `status='accepted'`, `accepted_by_applicant=True`, `accepted_at`, and `handoff_email_sent_at` (+ optionally re-sending `handoff_notification`) on a real `.save()` so simple-history records it. Build a proper ReDIB-coordinator action (button on Access Tracking / call-detail) that reinstates an `expired` (and probably `declined_by_applicant`) application to the "Active" end-state, with a **mandatory reason field** written to `history_change_reason` and the acting user captured via `_history_user`, and a choice of whether to re-fire the handoff email. Consider allowing the reverse transition through a dedicated service method rather than relaxing `VALID_TRANSITIONS` globally, so the terminal-state guard stays strict for the normal flow. Affected: `applications/models.py` (transition table / a guarded reinstate method), `applications/views.py` (new action + `_send_handoff_email` reuse), the Access Tracking / call-detail templates, and role decorators (`@coordinator_required`). **Done manually 2026-07-07:** reinstated REDIB-2601-015 to Active via shell (auto-expired 07-04 after the PI at CNIC started work without accepting); audit history attributed to the coordinator with a change reason. (Source: 2026-07-07 chat.) |
| 20 | High | **Coordinator report: bilingual per-call resolution table (EN + ES) for publication.** Once every resolution for a call is in, ReDIB must publish a results table listing each submitted application, its applicant organization, the node it was submitted to, and the node coordinator's resolution — in **both English and Spanish**, as two separate tables. There is no way to produce this from the portal today; on 2026-07-23 it was generated by hand from the prod DB via `manage.py shell`. Build it as a report on the coordinator dashboard (`reports/`), with per-call selection, both language variants, and export (at minimum copy-paste-able HTML/markdown; CSV/XLSX desirable) plus a machine-readable feed for item 21's public page. **Recipe used manually (encode this in the report):** (1) the call is `Call.objects.get(code='REDIB-2601')` — note the DB code is `REDIB-2601`, not the `ReDIB2601` form people say out loud; (2) rows = `Application.objects.filter(call=c).exclude(status='draft')` ordered by `code` (for REDIB-2601: 27 applications, 3 still drafts → 24 rows); (3) the resolution column comes from **`NodeResolution.resolution`** (the node coordinator's decision: `accept` / `waitlist` / `reject`), *not* `Application.status` or `Application.resolution` — the published resolution is independent of whether the applicant later accepted, so REDIB-2601-026 publishes as *Accepted* despite having gone `expired`; every REDIB-2601 application requested equipment at exactly one node, so one resolution per row, but the report must handle multi-node applications (one row per node, or a combined cell — decide); (4) label maps — EN `accept→Accepted`, `waitlist→Wait List`, `reject→Rejected`; ES `accept→Aceptada`, `waitlist→En espera`, `reject→Rechazada`; (5) organization cell = `application.applicant.organization` rendered `short_name · name` with the `·` separator, falling back to `name` alone when `short_name` is blank or identical to `name` (4 of 24 rows on this call: Universidad de Navarra, Hospital Universitario La Paz, Hospital Universitario de Guadalajara, Imaging La Fe) — note this is the *user profile's* organization, not the free-text `Application.applicant_entity`; (6) node cell uses the public display names **BioImaC / biomaGUNE / TRIMA / IIS La Fe**, mapped from `Node.code` (`BioImaC`, `CIC-biomaGUNE`, `TRIMA@CNIC`, `IIS-LaFe`) — this mapping doesn't exist anywhere in code yet and needs a home. REDIB-2601 result for regression-checking the report: 16 accepted, 7 wait list, 1 rejected. **Note for Ryan:** this procedure also has to go into the **ReDIB Operations Handbook** — publishing the resolution tables after every call is a network operating procedure, not just a portal feature, and it must be documented outside the portal too. (Source: 2026-07-23 chat.) |
| 21 | High | **Public "past calls" archive with published resolution tables.** Applicants and the wider network have no way to see previous calls once they close — the `/calls/` page is oriented to what's currently open. Add an archive of closed calls (likely an extension of the existing calls page/app rather than a brand-new one — e.g. an "Open / Past" split or a per-call public detail view) where each past call shows **the same details it displayed when it was posted** (dates, scope, participating nodes, available equipment, documentation/links, whatever the live listing rendered) **plus the resolution table** from item 20 for any call whose resolution is complete, in both English and Spanish. Design points to settle: how "resolution complete" is determined (per-call flag set by the ReDIB coordinator, probably the same gate as item 15's release action, rather than inferring from row counts); whether the archived call detail is a live render of current `Call`/`Equipment` rows or a frozen snapshot (equipment and node data drift after a call closes — a snapshot is likely more honest for a published record); public/unauthenticated visibility and what applicant-identifying information is safe to publish (the table is organization-level, not person-level, by design); and stable permanent URLs so the tables can be linked from redib.net and from emails. Depends on item 20 for the table generation. (Source: 2026-07-23 chat.) |
| 19 | Medium | **Reassess `User.is_active` vs `UserRole.is_active` — one concept or two?** Every role lookup in the app filters `UserRole.is_active=True`, but only two call sites also check the account itself (`evaluations/tasks.py:418` and `evaluations/views.py:210-215`, both patched after an evaluator opted out for a session: we set `is_active` FALSE in `data/users.tsv`, synced, which flipped **`UserRole.is_active`** — and the evaluator code wasn't looking at either account state, so nothing changed). The remaining ~20 role queries (`applications/views.py` ×11, `access/views.py` ×3, `evaluations/tasks.py:644`, `applications/services/node_resolution.py:169`, `core/views.py:54`, `core/forms.py:149`, `core/decorators.py:29`) look at the role flag only, so a deactivated **account** with a live role still receives task emails it can never act on — it just can't log in. Same trap in the other direction. Hit again on 2026-07-23 deactivating `bioimac@ucm.es` (BioImaC): both flags had to be set by hand, and `populate_redib_users.py:257-264` hardcodes `is_active: True` on every role it writes, so the TSV's `is_active` column reaches `User.is_active` (line 183) but can never *deactivate* a role — the row's `roles` cell had to be blanked to make the deactivation survive a reload. Two flags kept in sync by hand across two dozen sites is duplicate work that diverges silently. Decide the model and enforce it once: either (a) collapse to a single source of truth — account active/inactive gates everything, drop or derive `UserRole.is_active`; or (b) keep both with a crisp definition ("account = can log in", "role = holds this responsibility") and route **all** lookups through one helper (e.g. `UserRole.objects.active()` manager / `User.active_roles()`) that applies both conditions, plus a loader that can express role deactivation from the TSV. Whichever way, ban raw `.filter(role=…, is_active=True)` in app code. Related: item 13 (`populate_redib_users` sync semantics). (Source: 2026-07-23 chat + earlier evaluator opt-out.) |
| 22 | Medium | **Newsletter image handling — replace the manual base64 inliner.** The `newsletters` app serves the uploaded HTML file raw in an iframe and the `Newsletter.html_file` field requires a *self-contained* file ("inline CSS, no external assets"), so the only way to include images today is to base64-embed them as data URIs inside the single HTML file. For the June 2026 issue this was done with a throwaway build script (`/home/rtasseff/tmp/redibNewsletter/build_newsletter.py`): it downscales/compresses each PNG with ImageMagick and inlines them, producing a ~1.5 MB self-contained HTML. This works but is clumsy: editors can't author images through the admin, every edit means re-running a script outside the app, and the data-URI bloat makes the stored HTML large. Decide a proper workflow. Options: (1) a `NewsletterImage` inline model in the admin (upload images alongside the newsletter, reference by a stable `/media/...` URL — note the raw iframe serves at `/newsletters/<slug>/raw/`, so images must use root-relative or absolute URLs, not relative); (2) an upload-time processing step that accepts a folder/zip and auto-inlines + optimizes (productionize the throwaway script as a `manage.py` command); (3) a richer authoring model (structured fields / blocks) rendered server-side instead of dropping in pre-baked HTML. Whichever path, keep the iframe `X-Frame-Options: SAMEORIGIN` serving intact and confirm images load in both dev (Django serves `/media/`) and prod (Caddy serves `/media/`). (Source: 2026-06-01 chat — first newsletter, Option A chosen as the interim.) |
| 23 | Low | **Scientific-project guidance text: worked examples per field (blocked on Ángel).** The Batch 1 requirements doc (`~/tmp/protal_issues_20260401.md`, 2026-04-01) deferred issue #25: the applicant-facing guidance/placeholder text on the Step 1/3 scientific fields (`applications/forms.py` help_texts and placeholders, `templates/applications/wizard_step1.html`, `wizard_step3.html`) was to be improved with concrete example text once Ángel supplied approved examples. Nothing has arrived and the text is unchanged; leave as-is until examples exist, then it is a copy-only change (no model/migration). The same doc's other external inputs are already resolved: organization and funding-agency seed lists landed as `data/organizations.tsv` / `data/funding_agencies.tsv` (issue #26), and "contact ID" was explicitly dropped (batch1 plan, Resolved Decisions). (Source: 2026-08-17 review of the April requirements doc; every other item in it shipped in `fixes-batch-1`, merged `62d9180`.) |
| 26 | Low | **`ConsultRequest.emails_sent_at` means "queued", not "delivered".** `calls/views.py` stamps it whenever `.delay()` didn't raise, but `send_email_from_template` swallows missing/inactive-template and SMTP errors (returns False), so the coordinator list's "Not emailed" badge never shows for requests whose notifications actually failed. Either check the eager return value / `EmailLog` status, or rename the badge to "not queued" and add an EmailLog-backed "delivery failed" indicator. (Source: 2026-08-18 review of PR #33.) |
| 27 | Medium | **`CallForm` exposes the raw `status` dropdown — coordinators can bypass the Announce/Publish/Close guards.** `calls/forms.py` includes `status` in `Meta.fields` and `call_form.html` renders it, so a coordinator can hand-set `announced`/`open` on any call regardless of dates or equipment. The auto-open path now refuses expired calls (`2ee0c7c`), but the form should either drop `status` (transitions only via the action buttons) or validate transitions in `CallForm.clean`. Related to GitHub #23 (what happens when call dates are edited). (Source: 2026-08-18 review of PR #33.) |
| 28 | Low | **`applications/tasks.py:~338` passes datetime objects in Celery `context_data`.** Celery's JSON serializer delivers them to the worker as raw ISO strings, so the rendered email shows `2026-10-01T00:00:00+00:00` in prod while dev (eager) formats them. `calls/services.notify_call_audience` pre-formats dates now; do the same there and grep for other `context_data` datetimes. (Source: PR #33 notes.) |

## Tooling / data ingestion

| # | Priority | Item |
|---|----------|------|
| 13 | Medium | **`populate_redib_users` sync semantics.** The current loader (`core/management/commands/populate_redib_users.py`) is unsafe to re-run on prod for two reasons. (1) It unconditionally `user.set_password('changeme123')` on every row (lines 189-190), so any user who has set their own password loses it. (2) It uses `update_or_create` with TSV values winning unconditionally — so any field that drifted from the TSV after first load (e.g., a coordinator edited their phone in `/profile/`, or we toggled `is_active` directly via the shell to handle an ops issue like activating Benito on 2026-05-20) silently gets stomped on the next reload. Decide a conflict-resolution policy: who wins (TSV / DB / per-field), do we need a `--dry-run` mode that diffs and exits, do we only set the password for newly-created users, and should `update_or_create` switch to "only update fields that changed in the TSV" (read the prior TSV from a checked-in snapshot, diff, apply only the deltas). Until this is sorted, do targeted DB updates by hand for one-off ops changes. (Source: 2026-05-20 chat — activating bjrubio@ucm.es from the new TSV without running the loader.) |
| 6 | Medium | **xlsx → tsv conversion + QC tooling.** The reference TSVs in `data/` are produced by exporting from Microsoft Excel on Windows, which writes Windows-1252 (CP1252) text and CRLF line endings. Our loaders assume UTF-8, so non-ASCII names (e.g. `Asociación`, `Compostela`) crash the import on the first row. Today the workaround is a manual `iconv -f cp1252 -t utf-8` pass plus eyeballing column counts. Build a small `manage.py` command (or standalone script) that takes the source `.xlsx`, writes UTF-8 TSV, and runs file-specific QC. Specific checks observed in real data so far that need automated catching: encoding (force UTF-8 output); trailing-tab artifacts in headers; leading/trailing whitespace in cells; underscore-style phone-extension suffixes (e.g. `+34 91 ... _5404`); typos in enum-valued columns (e.g. `prelcincal` for `preclinical`, `Public Research Organisation (PRO)` exact-spelling check); separator drift (`,` used where `;` is required, e.g. `users.tsv` `areas`); ambiguous booleans (blank vs explicit TRUE/FALSE — confirm intent); FK lookups (`organization_name` → `organizations.tsv:name`; `roles` `node_coordinator:CODE` → `nodes.tsv:code`; `equipment.tsv` `node_code` → `nodes.tsv:code`); duplicate natural keys; required columns present. Should be runnable per-file (`--file organizations`) and as a batch. (Source: 2026-04-16 chat / real-data prep.) ; we need to deal with phone number extentions in the xls file (also xls thows an error for + becuase it thinks it is a formula, not sure how some field have it and others do not)|

## Test infrastructure

| # | Priority | Item |
|---|----------|------|
| 7 | Low | **8 tests blocked by `ProfileCompletionMiddleware`.** The middleware (`core/middleware.py`, added in batch-1) redirects logged-in users with incomplete profiles to `/profile/`. Several tests in `tests/test_design.py`, `tests/test_phase7_acceptance.py`, `tests/test_phase9_publications.py`, and `reports/tests.py` create users via `User.objects.create_user(username, email, password)` only — no `first_name`, `last_name`, `phone`, `organization`, or `position` — and so get redirected when they try to GET/POST a protected view. Fix: add a shared `make_complete_user()` helper (e.g., in `tests/__init__.py`) that fills the required fields, and update the affected `setUp` methods. (Source: 2026-04-16 chat / pre-merge review.) |
| 8 | Low | **3 tests blocked by `ManifestStaticFilesStorage`.** `tests/test_design.AuthPageRenderTest.{test_login,test_logout,test_signup}_page_renders` render templates that include `{% static 'images/Logo-ICTS-def-low.jpg' %}`. The configured storage backend requires a built `staticfiles.json` manifest, which the test environment doesn't produce. Easiest fix: override `STORAGES['staticfiles']['BACKEND']` to `django.contrib.staticfiles.storage.StaticFilesStorage` in test settings (or in those test classes via `@override_settings`). (Source: 2026-04-16 chat / pre-merge review.) |
